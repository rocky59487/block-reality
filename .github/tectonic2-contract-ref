# The tectonic2 commit whose contract/ this repository mirrors byte for byte.
# CI (N23-b) clones this ref and diffs the whole directory: if either side moves
# without the other, the build goes red instead of the two drifting apart.
# Update it in the SAME commit that copies a new contract/ across.
98bfbd9705ae3097663640ffd5f25ee3bc4d1f91
#
# ...and the contract hash at that commit. This second line is what makes the mirror
# check work WITHOUT a cross-repository clone: check_contract.py hashes every file under
# contract/ (all but README.md and check_contract.py itself), so two repositories whose
# pins agree with their own directories AND with each other are byte-identical where it
# matters. The clone-and-diff above is still run when CI can reach the other repository;
# this line is the leg that cannot be skipped for want of a token.
sha256 42a1b24c3c0b29e837779b046302ede3a0c0f8c1c429f7b9e6f7c72fbfa9dc1d
