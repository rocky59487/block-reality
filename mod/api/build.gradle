// No runtime dependencies. Nullability is a compile-time API contract only;
// callers still cannot reach a solver, Minecraft, or a JSON library through this API.
dependencies { compileOnlyApi 'com.google.code.findbugs:jsr305:3.0.2' }
