# v0.4 → v1 規劃：聚合語意，然後才是倒塌

**狀態：已裁決，未實作。** 四條產品裁決（2026-08-24）記在 `DECISIONS.md` D-029～D-032。
本檔是路線圖與設計；判準在實作開始前另行凍進 `GATES.md` 與 `verify.py`（鐵則 1）。

---

## 0. 這一版存在的理由（實測證據，v0.3b 發布版引擎）

玩家報告：「同片水泥被切塊、同根柱子被切，不符合自動成為一個結構的構想。」
對 `dist/br-sidecar.exe`（v0.3b）逐場景重現，全部成立，而且比描述的更糟：

| 玩家蓋的東西 | 模型實際變成 |
|---|---|
| 5×5 混凝土樑塊樓板（邊緣支承） | **40 根 1000 mm 短棒**；自重 221,314 N，**恰好是單向計重的 2 倍**（每格同時被 x、z 兩條 run 記重） |
| 2×2×6 厚柱 | **44 根 1000 mm 短棒**，縱橫皆被切 |
| 6×4 磚牆 | 38 根 1000 mm 短棒 |
| 4×4×2 疊兩層板材料樓板 | **32/32 unassigned，整個結構從分析裡消失** |
| L 型牆（板材料，用法正確） | **兩個獨立 island**，轉角一排 4 格 unassigned |
| 柱 + 樑同材質交會 | 柱斷成上下兩根 member（拓撲正確——交會處本該有節點；錯的是「一根柱子」的**物件身分**沒了） |

根因：抽取層只認得兩種形狀——1 維的線（樑材料 run）與恰好單層的面（板材料 2×2 方格）。
1000 mm 短棒 L/h = 1，正是不變式 1 自己劃的「樑理論不成立」線；`main.cpp` 的
plate-catalogue 註解早已描述過這個 grillage 缺陷，當時的答案是「板要用板 token」——
但玩家自然的蓋法（用混凝土塊堆牆、堆厚柱）全部踩中。

**為什麼先修判定再做倒塌**：算的歸算，演的歸演——演出層演的是判定層算出的失效集合。
在重量×2、短棒化、結構會消失的分解上蓋倒塌，是把錯的失效集合演得很漂亮。而且倒塌要
「整根柱子一起倒」，前提正是「一根柱子是一個物件」——聚合語意就是倒塌的地基。

---

## 1. 裁決（已定）

| # | 決策 | 內容 |
|---|---|---|
| D-029 | 材料類別與軸向宣告 | frame（鋼/鋼筋/木）帶方向、並排不融合；monolith（混凝土/磚）鄰接即融合 |
| D-030 | 體材料一格 = 1 m³ | 融合區域按真實幾何分解；質量恰好記一次 |
| D-031 | DYNAMIC 剛體影響範圍 | 傷害實體，不對其他結構施加荷載 |
| D-032 | 碎裂視覺軟依賴 | 自家事件流 + 自家 fallback；Physics Mod 有可用 API 才接 |

不變式 3 不受影響：**結構角色（類別）仍由玩家用材料宣告**；自動化的是已宣告類別內的
**幾何分解**，而幾何分解是決定性、可解釋、可 gate 的——這正是不變式 3 當年反對「形狀
語意反推」的三個理由的反面。

---

## 2. v0.4 設計

### 2.1 材料類別

| 類別 | 材料 | 語意 |
|---|---|---|
| **frame** | steel、rebar、timber | 桿件是玩家放的線。方塊帶**軸向**（BlockState axis，放置時如原版原木決定）；run 只沿宣告軸抽取。並排/交叉不融合（§7.4 規則 4 不變——未螺栓的兩根鋼樑並排本來就不共同作用） |
| **plate** | concrete_slab_200/150、steel_plate_20 | 現行薄板語意不變：宣告厚度與 1 m 方塊解耦 |
| **monolith** | concrete、brick | **同材質面相鄰即融合成一個結構物件**；一格就是 1 m³ 的該材料 |

frame 的軸向宣告順帶從根源殺掉 grillage 雙倍計重：一個格子只屬於它宣告的那一條 run，
不再有第二條 run 搶同一格。蓋成面而軸向交錯的鋼樑陣照樣是格柵——但每格只記一次重，
而且那是玩家宣告出來的格柵，不是程式猜出來的。

既有的 `concrete_rect_400x600`（素混凝土「樑」）與 `brick_rect_230x350` 改為 monolith
——它們的 token 撒謊的部分（斷面與方塊解耦）本來就只對 frame 成立。v0.x 不承諾存檔相容，
照登在 release notes。

### 2.2 monolith 的融合與幾何分解

融合：同材質、面相鄰（6-conn）的 monolith 方塊集合為一個**區域**。對每個區域：

1. **稜柱判定**（整體）：沿某軸所有截面全等 → 整個區域是**一根 member**，斷面 =
   單位正方形聯集的真實斷面（2×2 柱 = 2 m × 2 m；L 型截面也成立）。這直接回答
   「同根柱子被切」。退化防護：稜柱長度須 ≥ 截面最大邊，否則落到下一層。
2. **逐格厚度分類**（否則）：每格沿三軸各量區域內 run 長 →
   恰一軸厚度為 1 = **面狀**；兩軸厚度為 1 = **線狀**；其餘 = **塊狀**。
   - 面狀連通子區域 → **MITC4 網格**，厚 1000 mm。轉角格（沿兩個法向厚度皆 1）
     **同時屬於兩片牆的網格**——L/T 牆因此在轉角共節點，folded plate 是標準解法。
     轉角格質量在兩片間均分（鏡像對稱要求；50/50 是唯一對稱解）。
   - 線狀子區域 → member（1 m × 1 m 斷面）。
   - 塊狀殘餘 → **剛體質量塊**：質量正確、透過介面節點傳力、不報內應力、
     在回覆裡明確列報（新 `bulk` 區段），**永不靜默消失**。
3. **介面**：子區域之間、與相鄰 frame/plate 元素之間，靠共用節點（方塊中心/面，
   沿用 D-028 半格單位）連接。

### 2.3 任意斷面性質

單位正方形聯集斷面：A、Iy、Iz、形心是精確整式。**扭轉常數 J** 用 St. Venant
（Prandtl 應力函數）在斷面格點上做小型 FD 解，對矩形 β 表閉合值 gate 收斂。
剪力面積 Av 同批。短胖 member（L/h < 3，例如 1×1×2 的墩）啟用 FrameCore 的
opt-in Timoshenko，並在回覆帶適用性註記（INV-6 的路線，這次真的接出來）。

已查證（M0）：FrameCore 的 `Section` 是開放 struct——A/Iy/Iz/J/cy/cz/**Asy/Asz**
（Timoshenko 剪力面積）/Zy/Zz（塑性模數，v1 塑鉸要用）全部可直接填，`shape` 只決定
雙軸彎曲的合成法。聯集斷面沿用 `Rectangular` 的最壞角落和——對非凸斷面偏保守，
方向誠實（保守），照註記。

### 2.4 厚殼的適用界線

1 m 厚的 MITC4 在 t/L 大時是 Mindlin 板，不是 Kirchhoff——這是 MITC4 的主場，但要
gate：對 3D 彈性精確解（厚板基準，文獻對照見 §7）驗收 t/L = 1/4 附近的行為，並把
「t/L 超過界線讀成指示性」寫進註記，與 INV-6 同一格式。

### 2.5 物件身分

**融合區域（或 frame 的一條宣告 run）= 遊戲側的一個「構造物件」**；member/shell/bulk
是它內部的離散化。hover、`/br status`、未來倒塌的脫離單位，都以構造物件為單位。
柱+樑交會把柱切成兩個元素的事實不再滲漏到玩家面——玩家看到的是一根柱，帶一條
沿長度的 D/C 曲線。這也是 MEMBER_SEMANTICS §9.5「member 是持久物件」真正的落地點。

### 2.6 挫屈效能（隨行，處置紀錄點名項）

遊戲側依節點數門檻（config，預設暫定 400）自動關挫屈（請求層 `"buckling"` 已存在），
玩家面顯示「挫屈未評估（結構規模）」。`bucklingFactor = 0` 的三態語意 v0.3b 已修好，
這裡只是接上開關。

### 2.7 協定變更

blocks 條目新增 `axis`（frame 用）；回覆新增 `bulk` 區段與 `unassigned` 的
**理由碼**（今天只有座標，玩家不知道為什麼消失）。JSON 與 shm 兩條 wire 同步改，
協定版本號 bump，`verify.py` 的 shm 解碼器同步。

---

## 3. 判準（實作開始前凍結；此處是清單，正式條目進 GATES.md）

| # | 判準 | 綁什麼 |
|---|---|---|
| N1 | **質量恰一次**：`applied == ρ·V·g`，rel ≤ 1e-12 | 樓板、牆、厚柱、L 牆、含塊狀殘餘的混合區域，全部場景 |
| N2 | 水平鏡像與 90° 旋轉後全量不變；上下鏡像幾何與質量不變 | [M1] 的形式，作用於新分解 |
| N3 | 2×2 與 L 型稜柱斷面性質對手算精確值 | A、Iy、Iz、形心 |
| N4 | J 解算器對矩形 β 表；網格加密收斂階 | 閉合解 |
| N5 | 厚板 MITC4 對 3D 彈性基準 | 文獻數值表 |
| N6 | L 牆是 **1 個 island**，轉角傳力（兩腳反力和 = 總重） | 修掉今天的 2-island |
| N7 | **分解完備性**：每格恰屬 member/shell/bulk/unassigned(帶理由) 之一 | 不再有靜默消失 |
| N8 | 決定性 8/8、雙平台一致 | 沿用現行程序 |
| N9 | 挫屈門檻行為（開/關/訊息） | Java 側 |
| N10 | 短柱 Timoshenko 對厚樑閉合解 | 撓度含剪切項 |

紅 → 綠的順序照鐵則 1；任何事後移線登記在 `GATES.md` 並降級下游結論。

---

## 4. 里程碑

| | 內容 | 規模 |
|---|---|---|
| M0 | 文獻對照 + **FrameCore 能力查證**。**2026-08-24 已做大半**：MPC 無（→R1）、Timoshenko 有（`SolveOptions.useTimoshenko` + `Asy/Asz`）、倒塌驅動器齊備（§5）。剩：殼在折面的 drilling 行為實測、文獻第二批（J 解法、厚板基準） | S，收尾 |
| M1 | 判準凍結（GATES.md + verify.py 紅測試） | M |
| M2 | 協定 + frame 軸向（Java BlockState、放置 UX、wire） | M |
| M3 | monolith 融合 + 分解核心（稜柱/面/線/塊分類、介面節點） | **L，主戰場** |
| M4 | 斷面引擎（聯集斷面 + J 解算器 + Timoshenko 接出） | M |
| M5 | 殼縫合（轉角雙屬、質量均分、厚殼註記） | M |
| M6 | 遊戲側（構造物件身分、bulk 顯示、unassigned 理由、挫屈開關） | M |
| M7 | 文件（MEMBER_SEMANTICS 增補、README、LISTING）+ 發布 v0.4 | S |

---

## 5. v0.5 / v1：倒塌（裁決已定，設計待 v0.4 落地後細化）

**M0 已查證（2026-08-24，讀 FrameCore v4 公開頭檔）：判定側的重活引擎已經做完了。**

| 能力 | FrameCore 現況 |
|---|---|
| 漸進倒塌 | `Collapse.h`：GSA 突然移除 + DLF、removeThreshold 迴圈、**塑鉸（Mp = fy·Z）**、N-M 交互折減，誠實邊界寫明（sequential linear，非真彈塑性） |
| 動態倒塌 | `DynamicCollapse.h`：Ritz/模態基底時間積分、事件預算、**FragmentCluster 帶脫離瞬間的速度/角速度**、逐幀 callback 串流 + 取消——正是 Q10「權威流重放」要的形狀 |
| 崩塌即時重解 | `Reanalysis.h`：Tier-1 **精確 Woodbury**（rank ≤ 96）/ Tier-2 PCG / Tier-3 rebaseline，機構從因子的 capacitance 奇異性偵測（D-008 的宣稱屬實） |
| 接合玩法 | `Member.release[12]` + `enableReleases`（靜態凝聚）——§7.6 焊接/螺栓語意的引擎端已就緒 |

所以 v0.5/v1 的判定側主要是**把引擎既有驅動器接出 sidecar**（wire 格式、決定性、事件流
落到承諾軌），不是從頭寫倒塌迴圈。

**v0.5 — 判定與基本演出**
- 接出靜態倒塌驅動器：D/C ≥ 1 → CollapseOptions 迴圈 → 每步一筆**決定性事件**
  （構造物件、失效模式、脫離的方塊集合）；事件流是承諾軌輸出（不變式 6）。
- 演出最小版：脫離集合以 FallingBlockEntity 群組墜落。演出永不回饋判定。

**v1.0 — 剛體、碎裂、整合**
- 構造物件走 §9.5 生命週期 `STRUCTURAL → DETACHING → DYNAMIC → RESTING`。
  DYNAMIC = 伺服器權威簡化剛體（箱體對體素世界碰撞、翻滾），**傷害實體、
  不對其他結構施加荷載**（D-031）。初速直接取 FragmentCluster 的 vel/angVel——
  脫離瞬間的運動學是判定層給的，不是演出層編的。
- 碎裂判定屬判定層：脆性材料（混凝土/磚，張力/剪力模式）碎成逐格碎塊，
  延性材料（鋼/木）整根脫離。碎塊本身是**純客戶端視覺**（Q10 條款）。
- Physics Mod 串接（D-032）：先 spike 驗證其 API 可掛性；自家 fallback
  （FallingBlockEntity + 粒子）任何情況都在。上架頁不加前置依賴。

---

## 6. 風險與未決

| # | 風險 | 對策 |
|---|---|---|
| R1 | **已查證：FrameCore 沒有剛性連結/MPC 元素**（公開頭檔無此物；grep 到的 rigid 都是碎塊/板帶語彙）。bulk 介面若用高剛度假樑會傷條件數 | 二選一：patch 系列加 MPC（已有三個 patch 的先例），或高剛度元素 + pivot ratio gate 盯條件數；M1 凍 gate 時定案 |
| R2 | J 的 FD 解算器是新數值元件 | N4 閉合解 + 收斂階 gate；與既有 SHEAR/TORSION 零 oracle 缺口一起處理，順序：先查 FrameCore 的 τ 公式再動數字 |
| R3 | 稜柱判定的退化（立方體沿三軸都是稜柱） | 長度 ≥ 截面最大邊；tie 落到塊狀。鏡像/旋轉 gate 盯裁決對稱性 |
| R4 | t/L → 1 時 Mindlin 本身失效 | N5 定界線，超界讀成指示性並註記 |
| R5 | Physics Mod 閉源，API 未知 | spike 限時；結論寫進 D-032 的否證欄 |
| R6 | 大牆的殼數量 × 挫屈成本 | §2.6 門檻先擋；挫屈演算法升級（子空間法）留給下一輪 |

**非目標（v0.4 明確不做）**：鋼筋混凝土複合斷面（工法軌）、模板/澆置/養護狀態機、
frame 材料的「肉」（混凝土包鋼）、自由模式對**非結構**原版方塊的推導、任何實體元素求解。

---

## 7. 既有方法對照（M0 文獻掃描，2026-08-24）

**存在性證明**：voxel → 梁+殼理想化有一條完整驗證過的先例管線——骨小樑生物力學的
**ITS-based Plate-Rod FE 模型**（Columbia）：二值體素 → thinning 骨架 → DTA 拓撲分類
（curve/surface）→ 完整體積分解（每個體素標記屬於哪片 plate 或哪根 rod）→ shell + beam
取代，模型尺寸縮小約 762 倍，模量與降伏強度對體素 FE 高度相關。**可行性來自輸入性質
（骨頭處處是薄的），不是演算法通用性**——這正是 D-030 把自動分解限定在 monolith 類別、
塊狀殘餘落到剛體塊的理由：與其在厚塊上硬造模型，不如判定「這不是薄的」然後誠實處理。

**方法選型（三條結論，直接約束 M3 實作）**：

1. **走距離場路線，不走 thinning 路線。** 精確平方 Euclidean 距離轉換
   （Saito–Toriwaki / Felzenszwalb–Huttenlocher）在整數格點上是精確整數運算——
   決定性與鏡像不變免費；局部厚度（Hildebrand & Rüegsegger）就是 CAD 降維
   「厚度比判準」在體素上的直接對應。反之 subiteration thinning 依固定方向順序掃描，
   有方向偏置，鏡像不變性不保證。§2.2 的逐軸 run 長分類是局部厚度的軸對齊簡化版，
   與此路線相容。
2. **線/面分類的嚴格版已存在**：Saha 等人的 digital topological analysis（DTA）
   用 3×3×3 鄰域的 tunnel/cavity 數把骨架體素分成八類（曲線/曲面/端點/接頭）——
   純局部、基於拓撲不變量。v0.4 的簡化分類若在畸形輸入上出現裁決歧義，這是升級路。
3. **業界公認的難點在接頭不在分類**：CAD midsurface 工具（CADfix、HyperMesh）的
   失效模式是 ill-connected midsurface（缺片、縫、重疊），修補以天計。本計畫把
   L/T 縫合（§2.2、N6）與 bulk 介面（R1）列為主戰場，與文獻的教訓一致。

**任何貪婪掃描（含 maximal-box 分解）的 x→y→z 順序都會破壞鏡像不變**——除非裁決
正則化。這把 N2 從「驗收項」升格為「設計約束」：每一條分解裁決都要先問「鏡像後這條
裁決還做同一個決定嗎」。轉角質量 50/50 均分是這個約束的第一個實例。

**遊戲界對照（查證於 2026-08-24）**：Medieval Engineers 是逐方塊應力啟發式 + 連鎖崩塌
（演算法未公開）；Space Engineers 明確不做；Teardown 是程序化碎裂非結構分析；
既有 Minecraft 結構 mod（Realistic Block Physics 等）是鄰居計數/支撐距離啟發式，
無勁度矩陣；唯一真 FE 的 Minecraft 案例（Purdue 教學工具）是體素即元素的連續體，
自承粗解。**對外文案的合規寫法**：「據我們所查，尚無遊戲以梁殼理想化的 FEA 做結構
判定」——帶查證日期，不寫絕對句；M7 時進 check_docs.py 禁句表管理。

（斷面 J 解算器的方法對照與厚板基準解表：文獻掃描第二批，回來後補入 §2.3/N4/N5。）
