# v0.4 → v1 規劃：聚合語意，然後才是倒塌

**狀態：兩輪裁決完畢，開工。** 產品裁決記在 `DECISIONS.md`：D-029～D-032（2026-08-24）、
**D-033（力學只住引擎側）、D-034（換裝為主軸）與 D-030 修訂（bulk 改誠實拒絕）**
（2026-08-25，本檔 §8 是 PR #60 審核八條的處置紀錄）。
判準在實作開始前另行凍進 `GATES.md` 與 `verify.py`（鐵則 1）；
聚合抽取的實作位置依 D-034 是 **tectonic 的 mc lane**，照該倉的判準先凍紀律走。

---

## 0. 這一版存在的理由（實測證據，v0.3b 發布版引擎）

玩家報告：「同片水泥被切塊、同根柱子被切，不符合自動成為一個結構的構想。」
對 `dist/br-sidecar.exe`（v0.3b）逐場景重現，全部成立，而且比描述的更糟：

| 玩家蓋的東西 | 模型實際變成 |
|---|---|
| 5×5 混凝土樑塊樓板（邊緣支承） | **40 根 1000 mm 短棒**；自重 221,314 N，**恰好是單向計重的 2 倍**（每格同時被 x、z 兩條 run 記重） |
| 2×2×6 厚柱 | **44 根 1000 mm 短棒**，縱橫皆被切 |
| 6×4 磚牆 | 38 根 1000 mm 短棒 |
| 4×4×2 疊兩層板材料樓板 | **32/32 unassigned，整個結構從分析裡消失** |
| L 型牆（板材料，用法正確） | **兩個獨立 island**，轉角一排 4 格 unassigned |
| 柱 + 樑同材質交會 | 柱斷成上下兩根 member（拓撲正確——交會處本該有節點；錯的是「一根柱子」的**物件身分**沒了） |

根因：抽取層只認得兩種形狀——1 維的線（樑材料 run）與恰好單層的面（板材料 2×2 方格）。
1000 mm 短棒以**各自宣告斷面**算 L/h = 1.67（concrete 400×600）／2.86（brick 230×350）
／2.50（steel 200×400）——離 Euler-Bernoulli 要的 ≳10 都很遠，樑理論照樣不成立
（二審 F 更正：本段原寫 L/h = 1，那是 1 m³ 實心的比值——正是不變式 2 說這些 token
**不是**的東西）；`main.cpp` 的 plate-catalogue 註解早已描述過這個 grillage 缺陷，
當時的答案是「板要用板 token」——但玩家自然的蓋法（用混凝土塊堆牆、堆厚柱）全部踩中。

**為什麼先修判定再做倒塌**：算的歸算，演的歸演——演出層演的是判定層算出的失效集合。
在重量×2、短棒化、結構會消失的分解上蓋倒塌，是把錯的失效集合演得很漂亮。而且倒塌要
「整根柱子一起倒」，前提正是「一根柱子是一個物件」——聚合語意就是倒塌的地基。

---

## 0.1 第二批實測（2026-08-29，對已發布的 v0.3c 引擎）

玩家再報三件事：「方塊突然不參與計算」「混合結構沒有計算到」「超出一定距離會無法同時
計算」。逐條重現（`sidecar/repro_symptoms.py`，對 `dist/br-sidecar.exe`），三件全部成立，
**而且沒有任何 open issue 涵蓋它們**——已補開 #74、#75。

### (a) 未載入 chunk：靜默截斷，倖存者拿到自信的錯誤答案（#74，**P0**）

`StructureManager.visitForCycle:468` 的 `if (!level.isLoaded(pos)) return;`
**不載入未載入的 chunk 是對的**（強制載入會讓背景結構把 chunk 拖進來）；錯的是接下來
**什麼都沒發生**：方塊不進請求、不留紀錄，請求照送、回覆照收。

門型構架，梁中央 100 kN：

| 送進引擎的 | members | islands | singular | max D/C | `ok` |
|---|---|---|---|---|---|
| 完整 17 格 | 4 | 1 | 0 | **0.0606** | true |
| 右柱在未載入 chunk | 3 | 1 | 0 | **0.1927 (+218%)** | true |

世界沒有變，只有請求被切了一半，而回覆裡沒有一個欄位說模型不完整。反方向同樣糟：
10 格柱少了底部 3 格 → 0 members、`singular=1`，好端端接著地的柱子被報成機構。

（引擎在它看得見的範圍內是有守住的：當**載重所在的方塊**沒到時回 `ok=false`。
它看不見的是「結構少了一塊」。）

**這不是 N11。** N11 是載入**順序**不變性，假設所有方塊終究會到；本條是有些永遠不會到。

### (b) member 側面 ↔ shell 不接合（#75，P1）

先排除一件事：**材料混合是好的**。木梁架磚墩 = 3 members / 1 island / D/C 0.0966；
鋼柱 + 混凝土梁 = 2 members / 1 island。D-028 面節點確實有效。

出問題的是構件類與板殼類混在一起：

| 案例 | members | shells | unassigned | islands | singular |
|---|---|---|---|---|---|
| 四根三格柱 + 5×5 板樓板 | 4 | **16** | 0 | **1** | 0 |
| 兩端接地的梁 + 其上 5×3 板 | 2 | **0** | 0 | **2** | **1** |
| 同上，板只有一排寬 | 2 | 0 | **5** | 1 | 0 |

柱**端點**碰板會接；梁**沿長度**貼在板下不會接——接頭規則只連端點，不連側面。
而「梁在樓板下方」是最常見的真實工法之一。一格寬的板條不成 facet（合理），
但玩家得到的是什麼都沒有、也沒有理由：`unassigned` 有回傳，客戶端沒有顯示。

### (c) 挫屈由網格支配（#65，2026-08-28 已開，v0.3c 跟進）

19 m 素柱受自重：單元素 λ_cr = 3.1376，Greenhill 精確 9.8914（**低 68%**）；
2/4/10/19 元素 → 5.28/7.05/8.55/9.15。同柱頂載 > 自重 400×（軸力近均勻）時單元素距
Euler **0.5%**。半高一個**一牛頓**荷載讓 λ_cr 跳 68%。

v0.3c 已做的是**撤回錯誤宣稱**（「上界」一詞從六份對外文件移除，D-016 就地加註，
GATES.md 登記缺口），**沒有動網格**——依鐵則 1，元素細分是網格判準，要先凍再做，
而本版的聚合抽取本來就必須裁決細分。

### 這三條怎麼排

(a) 是 **P0 且屬 Java 側編排**（請求編排是 Java 的四件事之一，不違反 D-033），
與換裝進度無關，可獨立先做。(b)(c) 是抽取層與網格，住引擎側，併入 T 軌。

---

## 1. 裁決（已定）

| # | 決策 | 內容 |
|---|---|---|
| D-029 | 材料類別與軸向宣告 | frame（鋼/鋼筋/木）帶方向、並排不融合；monolith（混凝土/磚）鄰接即融合 |
| D-030 | 體材料一格 = 1 m³ | 融合區域按真實幾何分解；質量恰好記一次。**2026-08-25 修訂：塊狀殘餘改誠實拒絕（未接地）/ 地（接地），剛體塊列引擎能力需求** |
| D-031 | DYNAMIC 剛體影響範圍 | 傷害實體，不對其他結構施加荷載 |
| D-032 | 碎裂視覺軟依賴 | 自家事件流 + 自家 fallback；Physics Mod 有可用 API 才接 |
| D-033 | 力學只住引擎側 | Java 零力學；adapter 不仿造引擎缺的能力；gates 綁 wire 引擎中立 |
| D-034 | 換裝為主軸 | br-sidecar 改薄宿主（Java 不動）；雙引擎並跑對數；聚合抽取寫進 tectonic mc lane |

不變式 3 不受影響：**結構角色（類別）仍由玩家用材料宣告**；自動化的是已宣告類別內的
**幾何分解**，而幾何分解是決定性、可解釋、可 gate 的——這正是不變式 3 當年反對「形狀
語意反推」的三個理由的反面。

---

## 2. v0.4 設計

### 2.1 材料類別

| 類別 | 材料 | 語意 |
|---|---|---|
| **frame** | steel、rebar、timber | 桿件是玩家放的線。方塊帶**軸向**（BlockState axis，放置時如原版原木決定）；run 只沿宣告軸抽取。並排/交叉不融合（§7.4 規則 4 不變——未螺栓的兩根鋼樑並排本來就不共同作用） |
| **plate** | concrete_slab_200/150、steel_plate_20 | 現行薄板語意不變：宣告厚度與 1 m 方塊解耦 |
| **monolith** | concrete、brick | **同材質面相鄰即融合成一個結構物件**；一格就是 1 m³ 的該材料 |

frame 的軸向宣告順帶從根源殺掉 grillage 雙倍計重：一個格子只屬於它宣告的那一條 run，
不再有第二條 run 搶同一格。蓋成面而軸向交錯的鋼樑陣照樣是格柵——但每格只記一次重，
而且那是玩家宣告出來的格柵，不是程式猜出來的。

**frame 接頭規則（補完，PR #60 審核 #5）**——每格只有一個軸，接合走兩條路：

- **跨軸交會**（T/L/X）：run 端點面相鄰於另一軸的 frame 方塊時，延伸到**該方塊的中心**
  共節點，並在該處切分對方的 run。X 型 = 兩條同軸 run 從兩側各自延伸到中央方塊中心，
  全部共用同一節點；剛性節點傳彎矩，連續性成立。單側延伸，無方向歧義。
- **同軸對接**（斷面或材料變更、端對端）：兩條 run 的節點都放在**共享面**上（D-028，
  半格座標）。絕不採「互相延伸到對方中心」——tectonic 現行 `extract.h` 正是這條規則，
  程式追蹤顯示它產出兩根重疊一公尺、不共任何節點的成員（§7.5，換裝前要修）。

既有的 `concrete_rect_400x600`（素混凝土「樑」）與 `brick_rect_230x350` 改為 monolith
——它們的 token 撒謊的部分（斷面與方塊解耦）本來就只對 frame 成立。v0.x 不承諾存檔相容，
照登在 release notes。

### 2.2 monolith 的融合與幾何分解

融合：同材質、面相鄰（6-conn）的 monolith 方塊集合為一個**區域**。對每個區域：

1. **稜柱判定**（整體）：沿某軸所有截面全等 → 整個區域是**一根 member**，斷面 =
   單位正方形聯集的真實斷面（2×2 柱 = 2 m × 2 m；L 型截面也成立）。這直接回答
   「同根柱子被切」。**條件（兩輪審核後定形）**：走 member 須同時滿足
   (a) 長度 ≥ 2 × 截面最大邊，且 (b) **截面長寬比 ≤ 3**——二審 C 指出初版的
   「兩維皆 ≤ 2」會把 3×3×20 的方墩推進塊狀，接地時整根 20 m 被靜靜當成地，
   比拒絕更糟。案例：6×4×1（截面 4×1，長寬比 4）→ 牆；10×4×1 → 牆；
   2×1×8、2×2×6、**3×3×20** → 柱 ✓。(a) 的**等號邊界**（2×2×4 恰滿足）
   凍門檻時一併寫死。門檻值 T2 凍定，鏡像/旋轉 gate 盯裁決對稱性。
2. **逐格厚度分類**（否則）：每格沿三軸各量區域內 run 長 →
   恰一軸厚度為 1 = **面狀**；兩軸厚度為 1 = **線狀**；其餘 = **塊狀**。
   - 面狀連通子區域 → **MITC4 網格**，厚 1000 mm。轉角格（沿兩個法向厚度皆 1）
     **同時屬於兩片牆的網格**——L/T 牆因此在轉角共節點，folded plate 是標準解法。
     轉角格質量在兩片間均分（鏡像對稱要求；50/50 是唯一對稱解）。
     **轉角剛度（審核 #2）**：兩片網格各止於**中面交線**，不得雙雙鋪滿整個轉角體積；
     殘餘的重疊剛度照登為**轉角帶 indicative 級**——質量精確、力平衡精確（N6 不受
     影響），剛度在轉角一格內偏硬，白紙黑字。
   - 線狀子區域 → member（1 m × 1 m 斷面）。
   - 塊狀殘餘（D-030 修訂，2026-08-25 重裁）：**未接地** → `unassigned` +
     理由碼 `BULK_UNSUPPORTED`，質量不進模型但體積/質量列報，**永不靜默消失**；
     **接地** → 是**地**（v0.3b 墊塊語意的推廣）：固定面相鄰元素的介面節點，
     列報 `bulk-ground`——厚基座上蓋牆仍可分析。剛體質量塊改列引擎能力需求。
     **bulk-ground 要有規模上限**（二審 C：無上限的「作地」會靜靜把玩家想檢查的
     結構變成邊界條件；候選門檻：垂直延伸 ≤ 2 格的墊層才算地，超過→拒絕並列報。
     T2 凍定）。
3. **介面**：子區域之間、與相鄰 frame/plate 元素之間，靠共用節點（方塊中心/面，
   沿用 D-028 半格單位）連接。

### 2.3 任意斷面性質

單位正方形聯集斷面：A、Iy、Iz、形心是精確整式。**非對稱斷面（L 型等）必算慣性積
Iyz 與主軸（PR #60 審核 #3）**：對 2×2 慣性張量取特徵向量得主軸角，構件局部座標
旋轉到主軸後以主慣性矩進引擎（FrameCore `Member.refVec` 可任意定向，M0 收尾項
驗證；tectonic 的 `axisRot` 現為四分之一轉，主軸角是它的推廣需求）。極端纖維距
cy/cz 在主軸座標系重算。只給 Iy/Iz 不給主軸的 L 型斷面，彎曲答案是錯的——
gate N12 用 L 型稜柱雙向載重釘死。

**扭轉常數 J** 的設計（文獻掃描第二批的結論，2026-08-24）：

- **雙式夾擠自驗**：Prandtl 應力函數 FD（Dirichlet，互補能）從下方逼近、warping
  function（位能極小，sectionproperties 的方法，MIT 授權可抄）從上方逼近——兩式各跑
  一次得到不需外部參考值的區間。（嚴格界只對 conforming FEM 保證，FD 版在 M1 先實測
  再宣稱。）
- **主 gate 用 n×m 矩形的 Timoshenko 精確級數**（單位邊長）：1×1 = 0.1406、
  2×1 = 0.4574、3×1 = 0.7899、4×1 = 1.1232、2×2 = 2.2492、3×2 = 4.699——涵蓋所有
  矩形聯集，走同一條組裝路徑。**薄壁疊加式 Σ⅓bt³ 不得當 gate**：對 2×1 高估 45%，
  t/b≈1 的方塊型斷面完全不適用。
- **L/十字型無公認閉合解**：用 sectionproperties 收斂值當黃金值 + 雙式夾擠交叉驗證，
  在 GATES.md 登記為**參考值 gate（結論低一級）**，不是解析 gate。
- **凹角兩條鐵律**（M1 凍 gate 時升格為正式決策）：(1) 凹角奇異性使 L/十字的 J 收斂
  掉到 O(h^{4/3})——容差按斷面凸性分兩類寫，**凹斷面禁用 h² Richardson**；(2) 凹角的
  理論剪應力無限大，任何網格下的局部峰值都是網格相依值，**不得進入 D/C 判定**——
  扭轉 D/C 對凹斷面用斷面級量 + 保守係數。
- **剪力面積 Av 是慣例相依的**（Cowper ν=0.3 矩形 κ=0.8497 vs Timoshenko/Hutchinson
  0.8667 vs 教科書 5/6）——慣例在 M1 凍死一種（預設 Cowper 1966）寫進 GATES.md，
  否則日後必出「沒 bug 卻過不了」的假輸格。

短胖 member（L/h < 3，例如 1×1×2 的墩）啟用 FrameCore 的 opt-in Timoshenko，
並在回覆帶適用性註記（INV-6 的路線，這次真的接出來）。

已查證（M0）：FrameCore 的 `Section` 是開放 struct——A/Iy/Iz/J/cy/cz/**Asy/Asz**
（Timoshenko 剪力面積）/Zy/Zz（塑性模數，v1 塑鉸要用）全部可直接填，`shape` 只決定
雙軸彎曲的合成法。聯集斷面沿用 `Rectangular` 的最壞角落和——對非凸斷面偏保守，
方向誠實（保守），照註記。

### 2.4 厚殼的適用界線

1 m 厚的 MITC4 是 Mindlin 板。文獻掃描第二批的裁定：驗收**拆成兩層**，混在一起會把
理論誤差誤判成元素 bug（MITC4 的病在薄極限 locking，不在厚極限）：

1. **元素 gate（閉合解，容差緊）**：對 FSDT Navier 解——簡支方板正弦載重
   `w/w_Kirchhoff = 1 + 5.6398·(h/a)²`（κ=5/6、ν=0.3、m=n=1），在 t/L = 0.1／0.25／
   0.5／1.0 四點測**元素本身**。（此式 M1 入 GATES 前對 Reddy 的 FSDT Navier 章複核。）
2. **理論 gate（業界標準）**：NAFEMS **LE10** 厚板基準——規格公開完整，目標值
   σ_yy = −5.38 MPa 於指定點。Srinivas (1969) 3D 彈性表為第三層選配（付費牆，
   數值未取得前不引用）。

**t/L 門檻是會拒絕的閘門，不是註解**（判準先凍：事後才加就是移線）：暫定
**t/L > 1/4 的殼結果降級為指示性**並在回覆註記——Mindlin 的公認適用界在 1/10 量級，
1/4–1/5 是誤差數個百分點的邊緣，t/L → 1 的東西是塊體不是板。實際門檻值 M1 凍定。
對玩家的意義：一格厚的 monolith 樓板要跨得夠遠（≳5 格）才有定量 D/C，短跨讀成指示性
——這與真實工程「那不是板是塊體」的判斷一致。

### 2.5 物件身分、損傷粒度與 chunk 規則

**融合區域（或 frame 的一條宣告 run）= 遊戲側的一個「構造物件」**；member/shell
是它內部的離散化。hover、`/br status`、未來倒塌的脫離單位，都以構造物件為單位。
柱+樑交會把柱切成兩個元素的事實不再滲漏到玩家面——玩家看到的是一根柱，帶一條
沿長度的 D/C 曲線。這也是 MEMBER_SEMANTICS §9.5「member 是持久物件」真正的落地點。

**粒度（審核 #4——「整棟一物件太粗」）**：物件是聚合與呈現單位，**不是失效粒度**。
損傷與斷裂記錄在**元素與方塊層**（DamageRecord 掛方塊集合，§9.5 原設計）；倒塌時
按斷裂面把物件切成**新物件**，lineage 保 ID 穩定——與 tectonic 的 fragment 模型
（斷 = 方塊離世 + 連通分量成塊）同構。一整棟相連混凝土是一個物件，但它可以局部
壞、局部掉，掉下去的那塊有自己的新身分。

**chunk 規則（審核 #7）**：分析範圍 = **registry 記錄的融合區域（持久）**，不是
「當前載入的 chunk」。區域跨到未載入 chunk → 該物件標 stale、分析延後，**永不**
因載入狀態被截斷重分類。gate N11 釘載入順序不變性：同一世界、不同載入順序 →
同一分解。

### 2.6 挫屈效能（隨行，處置紀錄點名項）

遊戲側依節點數門檻自動關挫屈（請求層 `"buckling"` 已存在），玩家面顯示
「挫屈未評估（結構規模）」。~~**成本錨**（二審補）：由 1000 節點 72.8 s 與 ~n^3.1
擬合，400 節點 ≈ **4 秒**、200 節點 ≈ 0.5 秒~~——預設門檻值在 B4 凍定時要對著這組
數字選，不是憑感覺（初版暫定的 400 對「自動保持開啟」而言太貴）。

> **成本錨已撤回（2026-08-30，實測）**：那組數字是 `bopts.denseThreshold = 0`
> 強制稀疏特徵解**之前**量的，差約 **80 倍**。實測 300 格的樓板是 **27 ms** 不是 2 秒。
> 而且 blocks 不是成本的變數——同樣約 300 格，直樑 1.2 ms、樓板 27 ms、構架 82 ms，
> 跨 20 倍；連 dof 都不是（構架 2412 dof 要 1005 ms，樓板 2424 dof 只要 42 ms）。
> 完整的表與處置（預設 300 → 600）在 `GATES.md` 的 2026-08-30c 節。

**Wire 現況更正**（二審 E，實測證實）：v0.3b 修好的是 **key 恆在**；
`bucklingFactor = 0` 至今仍同時代表「未計算」與「有算但無正特徵值」——實測兩者
皆回 0，回覆裡也只有這一個 key。Java 自動關會再添第三種 0。B3 協定變更時加**顯式
理由欄**（enum：computed / no-positive-eigenvalue / disabled-by-request /
disabled-by-scale）；在那之前 0 的語意以請求端自知為準，任何文件不得再寫
「三態已可分辨」。

> **已落地（2026-08-30，N18）**：`bucklingState` 上了兩條 wire，實測四個狀態
> 全可達——`computed` / `no-positive-eigenvalue` / `not-eligible` /
> `disabled-by-request`，第五個 `disabled-by-scale` 由宿主代入（引擎那一側看不出
> 「沒問」是誰決定的）。多出來的 `not-eligible` 是實作時發現的第四種零：問了，
> 但沒有任何一座島有元素可跑特徵解。**「三態已可分辨」的禁令因此解除**，但要注意
> 現在是**四態**，寫三態一樣是錯的。兩條 wire 都有矛盾檢查（有因子必為 computed、
> 反之亦然），`verify.py [N18]` 五條、封包側六條。

> **2026-09-02 追記（D-040）**：換裝後是**五態**：`computed / no-positive-eigenvalue / not-eligible / not-eligible-scale / disabled-by-request`
> + **`solver-failed`**（Lanczos 未收斂；引擎不回未收斂的數）。`not-eligible-scale` 改由引擎回（`budgetDof`），宿主以每島 `nodes/dof` 決定預算
> （Q3(a)）；`bucklingBlockLimit` 對著 tectonic 的實測成本（MC66a-21 RECORDED）重選。回覆帶 `buckling.kind ∈ {eigen, screen}`——
> 寫「四態」自此也是錯的。N16-c 修訂（上界）見 GATES 2026-09-02。

### 2.7 協定變更

blocks 條目新增 `axis`（frame 用；語意 = tectonic `McBlock.axis`，日後 joint /
axisRot / fill / strength 同樣照抄它的欄位語意，D-033 (3)）；回覆新增 `bulk` /
`bulkGround` 列報與 `unassigned` 的**理由碼**（今天只有座標，玩家不知道為什麼消失）。
JSON 與 shm 兩條 wire 同步改，協定版本號 bump，`verify.py` 的 shm 解碼器同步。

**進度（2026-08-30）**：

| 欄位 | 狀態 |
|---|---|
| `unassigned` 理由碼 | ✅ 落地。改成**按理由分組**（一個理由一個字串，不是一格一份）——全支承筏逐格列報的成本已登記在案，逐格再背一份字串會讓它更糟 |
| `bucklingState` | ✅ 落地（§2.6，N18）。原規劃沒有這一欄，是做 N17 時發現同一個 0 的問題還沒關 |
| `axis` | ⬜ 未動。屬 B3 的放置 UX，與擷取無關 |
| `bulk` / `bulkGround` | ⬜ 未動。屬 v0.4 的新分解（D-030），本輪不產生 `BULK_UNSUPPORTED` |

協定 1 → 2、shm layout 1 → 2、封包 `PROTOCOL` "4" → "5"，三者同一輪 bump。
理由碼列舉在 wire 上是**開放**的（N17-c），所以 `BULK_UNSUPPORTED` 日後上線
不必再動一次協定。

> **2026-09-02 追記（D-043）**：協定 3 = **BSI v1**（`contract/`），不是 protocol 2 加欄位。本節的 `axis`（必填）、`bulk`/`bulkGround`
> （→ `unassigned` 的 `BULK_*` 碼，開放列舉）、理由碼全部在 BSI 裡有位置；`support:bool` 移除（D-039）。JSON/shm 兩條 wire 改為
> BSI 的 T-A/T-B/T-B′ 三種傳輸同一語意。對照表在 `docs/ENGINE_BOUNDARY.md` 與 `docs/SWAP_PROGRAM.md` §5。

### 2.8 邊界紀律（D-033，開發期檢查表）

- **Java 只做四件事**：請求編排、物件身分簿記、渲染、演出。code review 時看到 Java
  出現內力/斷面/失效/分解字彙 = 違規。
- **不繞道（D-037，2026-08-30 新增）**：引擎側能力一律開需求給 tectonic2。不新增 FrameCore
  patch，不在 br-sidecar 或 Java 補系統去繞過引擎缺口——**即使那不是 D-033 所禁的「仿造」**
  （例如自己決定細分策略）。每一個「補」都是換裝時要拆的耦合。方塊語意的翻譯不受此限，
  它本來就住 adapter。
- **adapter 不仿造**：sidecar 裡任何「引擎沒有、我先湊一個」的力學（高剛度假樑、
  假 MPC、自算應力）= 違規；缺 → 引擎側補或誠實拒絕。
- **gates 綁 wire**：新 gate 一律寫成「方塊進 → 回覆斷言」，不引用引擎內部符號；
  雙引擎並跑時同一套跑兩遍。**受測者是「薄宿主＋引擎」這一對，不是引擎裸身**
  （二審 §3）——對數紅的時候先查翻譯層，再懷疑引擎。

#### 稽核紀錄（2026-08-29）

掃過 `forge/`、`mod/api/`、`mod/core/` 的全部 Java，找內力/斷面/失效/分解詞彙與力學運算。
**計算面乾淨**：唯一像力學的算式是 `SectionDiagram.peakMagnitudeMpa()` =
`max(|topSigmaMpa|, |bottomSigmaMpa|)`，兩個值都是引擎給的，用途是**色階正規化**；
`StressRibbonBuilder.scenePeak` 同理取全場最大。這是渲染，不是力學。

兩處灰區，記名不放過：

| 位置 | 是什麼 | 判斷 |
|---|---|---|
| `StructureManager.isSupported:569-575` | 「下面那格是實心、非結構方塊 → 這格接地」——**Java 在裁決邊界條件**，而 wire 的 `support` 欄位是布林，隱含完全固定 | **灰**。它沒有*計算*任何東西，是在回答 wire 問的問題；但「什麼算接地、以什麼固定度」這條**規則**住在 Java。該函式自己的註解就寫著這條規則仍未定案。D-030 修訂已把 bulk-ground 語意移進引擎側，這條該一起走 |
| `BucklingPolicy` | 依方塊數決定**是否**跑挫屈 | **可接受**。是效能策略不是力學，且結果誠實回報「未評估」而不是偽裝成穩定 |

另記一筆風格問題：`isSupported` 用 `isSolidRender()` 判斷承載能力。那是**視覺遮蔽**述詞，
不是結構述詞。目前九個方塊踩不到差異，但玻璃、屏障、樹葉這類方塊會讓兩者分歧。
併入 B6 一起處理（同一個函式）。

---

## 3. 判準（實作開始前凍結；此處是清單，正式條目進 GATES.md）

| # | 判準 | 綁什麼 |
|---|---|---|
| N1 | **質量恰一次（限 monolith）**：`applied == ρ·V·g`，rel ≤ 1e-12。二審 D：frame 的 V 無良好定義（不變式 2——方塊不是斷面），照字面綁全場景會在第一天紅在錯的對象上，且誤觸 D-030 否證 (2) | monolith 樓板、牆、厚柱、L 牆；混合區域中的 monolith 部分 |
| N2 | 水平鏡像與 90° 旋轉後全量不變；上下鏡像幾何與質量不變 | [M1] 的形式，作用於新分解 |
| N3 | 2×2 與 L 型稜柱斷面性質對手算精確值 | A、Iy、Iz、形心 |
| N4 | J：矩形對 Timoshenko 精確級數（六點表）；雙式夾擠區間成立；凸斷面收斂 O(h²)、凹斷面 O(h^{4/3}) 分類容差；L/十字對 sectionproperties 黃金值（**參考級**）；Av 慣例凍定 | 閉合解＋參考值，分級 |
| N5 | 厚殼兩層：MITC4 對 FSDT Navier 閉合式（t/L 四點）＋ NAFEMS LE10（σ_yy = −5.38 MPa）；t/L 降級門檻行為 | 閉合解＋業界基準 |
| N6 | L 牆是 **1 個 island**，轉角傳力（兩腳反力和 = 總重）；質量精確、剛度重疊照登 indicative | 修掉今天的 2-island |
| N7 | **分解完備性**：每格恰屬 member/shell/bulk-ground/unassigned(帶理由碼) 之一 | 不再有靜默消失 |
| N8 | 決定性 8/8、雙平台一致 | 沿用現行程序 |
| N9 | 挫屈門檻行為（開/關/訊息） | Java 側 |
| N10 | 短柱 Timoshenko 對厚樑閉合解 | 撓度含剪切項 |
| N11 | **載入順序不變性**：同一世界、不同 chunk 載入順序 → 同一分解、同一回覆 | Java 側整合測試 |
| N12 | **L 型稜柱主軸**：非對稱斷面雙向載重對主軸手算解；未轉主軸的答案必須被抓出來 | 閉合解 |
| N13 | **帳目守恆，分軌**：monolith = 入模體積 ρ·V·g 精確；**frame = Σ ρ·A_宣告斷面·L_member**（L 依 D-028 為中心到中心/面）；被拒絕/作地的體積分開列報且對總 | 帳目守恆 |
| **N14** | **模型完整性**（#74）：**已於 2026-08-29 凍結，五條線的細目在 `GATES.md`**。二選一取「照解但標記」——因為 `structural` **永不遺忘**（沒有 per-chunk unload），整體拒絕會在真實世界永久關閉分析。N14-a 跳過必留紀錄；N14-b 截斷面 `T` 非空即標記上 wire；N14-c 鄰接 `T` 的構件 withhold D/C 與上色；**N14-d 完整解 vs 截斷解不得在無標記下相異**；N14-e `T` 空時逐位元不變 | Java 側整合測試 + wire 欄位 |
| **N15** | **member 側面 ↔ shell 接合**（#75）：兩端接地的梁 + 其上 5×3 板 = **1 個 island**，板出 shell、梁出 member，載重路徑穿過接面（梁的反力和 = 板的總重）。一格寬板條允許不成 facet，但**必須帶理由碼**進 unassigned | 修掉今天的 2-island；接上 N7 |
| **N16** | **挫屈網格收斂**（#65）：自重懸臂對 Greenhill `qL³/EI = 7.837`、頂載懸臂對 Euler `π²EI/(2L)²`，兩個閉合解；細分後**單調趨近**且指定元素數下的誤差有上限。凍結前不得再對外描述 λ_cr 的誤差方向 | 閉合解 |
| **N17** | **帳目完整性**（**已於 2026-08-30 凍結並落地**，七條線的細目在 `GATES.md`）：回覆必須為每一個輸入方塊指定去處，`unassigned` 與元素不相交，每一格帶**開放列舉**的理由碼，奇異島的方塊以 `MECHANISM` 進 `unassigned`（此前它們在回覆裡根本不存在），全支承與擷取失敗在玩家面是不同句子，理由碼不得描述力學 | wire 欄位 + 引擎側 gate |
| **N18** | **挫屈狀態**（**已於 2026-08-30 凍結並落地**）：回覆說得出 `bucklingFactor` 是哪一種狀態，狀態與因子不得互相矛盾。實作時發現是**四**態不是三態（多一個「問了但沒有可跑的元素」） | wire 欄位 |

紅 → 綠的順序照鐵則 1；任何事後移線登記在 `GATES.md` 並降級下游結論。

**N14/N15/N16 的凍結順序**：N14 與換裝無關（Java 側編排），先凍先做；N15 與 N7 是同一件事
的兩面（分解完備性），一起凍；N16 必須在 T3 決定細分策略**之前**凍，否則會變成「先做完再
補一條剛好通過的線」——那正是鐵則 1 存在的理由。

---

## 4. 里程碑（D-034 改組：雙軌）

**T 軌（tectonic 倉，照它的判準先凍紀律；聚合的長期家）**

| | 內容 | 規模 |
|---|---|---|
| T1 | **共線對接修正**（✅ 2026-08-25，tectonic PR #6 → main `44d7632`） | 完成 |
| T2 | **monolith 聚合判準凍結**（✅ `docs/specs/MC5_MONOLITH.md`，N1–N7/N12/N13 → MC43..MC53） | 完成 |
| T3 | **聚合抽取實作**（✅ `core/mc/monolith.h`） | 完成 |
| T4 | **斷面引擎**（✅ `core/mc/torsion.h`、MC47/MC48） | 完成 |
| T5 | **殼縫合**（✅ MC46/MC49；V1-FIX-1 FIX-E-2 另縫上 line×sheet 的 seam） | 完成 |
| **T6** | **member 側面 ↔ shell 接合**（N15，#75）。~~tectonic 宣稱 FIX-E-2 已縫 line×sheet——先驗證再相信~~ **驗證做完了（2026-08-30）**：br-sidecar 側走完整條接觸邊界，規律是「run 的端碰到板接得上，側面一律接不上」，而且缺的是**引擎能力**（無剛性連結／MPC／偏心），不是擷取器的疏漏。依 D-036 走引擎路線，需求已提 `tectonic2#13`。判準 N15-a..e 已凍，四紅一綠 | M（tectonic 側） |
| **T7** | ~~**挫屈細分策略**（N16，#65）：凍結後決定 run 何時切元素~~ **改題（2026-08-30，D-037）**：N16 已凍（`GATES.md` 2026-08-30d），而量測顯示這不是「切幾個元素」的題目——誤差 ≈ 140/n 的**一階**收斂，5% 要 ~28 個元素。正解是**沿桿變化的幾何剛度**，屬引擎能力，需求已提 `tectonic2#14`（與 `tectonic2#12` 是同一題的兩面，參考解共用）。**adapter 不做細分** | M（tectonic 側） |

T1–T5 五項在 tectonic tag `v1.2` 全閉（來源 #69 的自查，附 file:line）。
**但同一份查證帶回五個換裝阻擋項**（#69），其中一條是 **D/C 只取兩端 max、對跨中彎矩全盲**
——那是判定層的取樣盲點，比聚合更靠近「數字對不對」。換裝排程以 #69 為準，不以本表為準。

**B 軌（block-reality 倉；宿主、對數、玩家面）**

| | 內容 | 規模 |
|---|---|---|
| B0 | M0 收尾：`refVec` 任意定向驗證、折面 drilling 實測、§7 待確認清單 | S |
| B1 | **薄宿主**：br-sidecar 對內載入 frame_capi_v2 DLL（tectonic artifact）或沿用靜態 FrameCore；對 Java 的 wire 不變 | M |
| B2 | **雙引擎並跑對數**：同一套 verify 驅動兩模式、回覆層比對；桿系目標 MC32 級（1e-12），殼照它的帶級裁決記載。**釘住共線對接 fixture 記名排除**（D-035，清單 ≤ 3 類）——那裡的差異是兩份凍結慣例不同，不是引擎缺陷 | M |
| B3 | 協定欄位（axis 等，§2.7）+ Java BlockState 放置 UX | M |
| B4 | 遊戲側：構造物件身分、~~unassigned 理由碼顯示~~（✅ 2026-08-30，N17/N18：HUD 逐理由一行、`/br status` 逐理由一句、挫屈四態各有句子）、bulk-ground 顯示、挫屈門檻、chunk 規則（N11） | M |
| **B6** | **模型完整性**（N14，#74，**P0**）：`visitForCycle` 先**記錄**它跳過了什麼；依 N14 凍定的處置回拒絕或截斷標記；HUD 說得出「結構有一部分不在載入範圍內」（✅ 2026-08-29） | M |
| **B7** | **帳目完整性**（N17/N18，**P0**，✅ 2026-08-30）：B6 關的是輸入側的靜默截斷，這條關的是輸出側——奇異島的方塊在回覆裡**根本不存在**（V03A_REVIEW N4-2，實測 12 格憑空消失），而 `unassigned` 用一句話講三種互不相同的事。理由碼上 wire，玩家面逐理由分開講 | M |
| B5 | 文件 + 發布 v0.4（引擎=tectonic 或雙引擎並跑期，視 B2 對數結果） | S |

排序：**B6 先行**（P0，與換裝無關，且它修的是玩家現在就在遇到的錯數字）→
T6/T7 判準凍結 → B1/B2（換裝骨架）→（T6/T7 ∥ B3/B4）→ B5。

B6 提到最前面是因為它跟其他每一項都不衝突：它動的是 Java 的請求編排——D-033 明列的
Java 四件事之一——不碰力學、不碰抽取、換裝之後一行都不用改。而它現在正在讓玩家看到
高 218% 的 D/C。

**B6 的紅線**：處置只能是「拒絕」或「標記」。把截斷面當成支承是在 Java 端發明邊界條件，
違反 D-033，明確排除。

D-034 否證 (1) 的備援：若 T 軌拖過兩個發布窗，T2 凍結的同一套 gates 改在 br-sidecar 實作。

> **2026-09-02 追記（`docs/SWAP_PROGRAM.md` §6）**：T 軌續接 tectonic2 **v1.3**（側枝合流 + MC61/MC62 + CMake）→ **v1.4**（MC63/64/65a/65b/60d）
> → **v1.5**（MC66a/b）→ **v1.6**（MC68 BSI 實作、MC67）；判準 11 份已凍在其 `docs/specs/MC6*.md`。
> B 軌改形：**B1** 薄宿主 → 傳輸轉接 + 原始碼靜態連結（D-041，不是載 DLL）；**B2** 雙引擎對數 → 差異帳 N22 + 一致性語料 N23；
> **B3** 協定欄位 → **B8 BSI codec**（Java `BsiCodec` + sidecar T-B；D-043）；**B4** 挫屈門檻 → nodes/dof（Q3(a)）、bulk-ground 顯示 → `unassigned` 碼；
> 新增 **B9** Java 公式刪除（D-038、N20）與 **B10** 地面列舉 + 持久 registry（D-039、N21、#86）。
> 驗收：`verify.py` 除 `sidecar/expected_red.json` 帳上的腿外全綠；差異帳零 blocker。
>
> **2026-09-03 追記（D-044）**：B1「傳輸轉接 + 原始碼靜態連結」改為 **`InProcessEngine` 接線**（#89；引擎 = jar 內原生共享庫）；**B8 的 Java codec 已落地**（PR #88，N24-a/b 首跑）；
> B2 的雙引擎對數（N22）**無實作、無 issue**——FrameCore 的 BSI 臂要先裁決保留或降級（`docs/ALIGNMENT_LEDGER.md` A14/C1）；B5 發布多一項前提：Windows natives（#90）。
> T 軌：tectonic2 的 MC68 轉接器先於 v1.3 落地（能力空集合，其 #20），LINUX_BUILD 落地。

---

## 5. v0.5 / v1：倒塌（裁決已定，設計待 v0.4 落地後細化）

**M0 已查證（2026-08-24，讀 FrameCore v4 公開頭檔）：判定側的重活引擎已經做完了。**

| 能力 | FrameCore 現況 |
|---|---|
| 漸進倒塌 | `Collapse.h`：GSA 突然移除 + DLF、removeThreshold 迴圈、**塑鉸（Mp = fy·Z）**、N-M 交互折減，誠實邊界寫明（sequential linear，非真彈塑性） |
| 動態倒塌 | `DynamicCollapse.h`：Ritz/模態基底時間積分、事件預算、**FragmentCluster 帶脫離瞬間的速度/角速度**、逐幀 callback 串流 + 取消——正是 Q10「權威流重放」要的形狀 |
| 崩塌即時重解 | `Reanalysis.h`：Tier-1 **精確 Woodbury**（rank ≤ 96）/ Tier-2 PCG / Tier-3 rebaseline，機構從因子的 capacitance 奇異性偵測（D-008 的宣稱屬實） |
| 接合玩法 | `Member.release[12]` + `enableReleases`（靜態凝聚）——§7.6 焊接/螺栓語意的引擎端已就緒 |

所以 v0.5/v1 的判定側主要是**把引擎既有驅動器接出 sidecar**（wire 格式、決定性、事件流
落到承諾軌），不是從頭寫倒塌迴圈。

（本表的查證來源是本機 `C:/Users/wmc02/Documents/FrameCore-PFSF-v4-source` 的公開
頭檔——不在兩個倉庫內，二審無法覆核，照登為單方查證；tectonic 側的對應能力
（容量面/Fragment/Newmark/衝擊分診）有 ROADMAP v0.6alpha 列可對。）

**tectonic 側（2026-08-25 查證）更進一步**：M-MC3 已出貨**容量交互面 + worst-first SLA
級聯 + Fragment 交棒（RLE、質量/質心/慣性張量、初速）+ Newmark 動力 lane + 衝擊分診**，
全部經 wire 可觀察、MC25–MC31 全綠。換裝完成後，v0.5 的倒塌判定幾乎是「已在 wire 上」，
剩演出側。

**v0.5 — 判定與基本演出**
- 接出靜態倒塌驅動器：D/C ≥ 1 → CollapseOptions 迴圈 → 每步一筆**決定性事件**
  （構造物件、失效模式、脫離的方塊集合）；事件流是承諾軌輸出（不變式 6）。
- 演出最小版：脫離集合以 FallingBlockEntity 群組墜落。演出永不回饋判定。

**v1.0 — 剛體、碎裂、整合**
- 構造物件走 §9.5 生命週期 `STRUCTURAL → DETACHING → DYNAMIC → RESTING`。
  DYNAMIC = 伺服器權威簡化剛體（箱體對體素世界碰撞、翻滾），**傷害實體、
  不對其他結構施加荷載**（D-031）。初速直接取 FragmentCluster 的 vel/angVel——
  脫離瞬間的運動學是判定層給的，不是演出層編的。
- 碎裂判定屬判定層：脆性材料（混凝土/磚，張力/剪力模式）碎成逐格碎塊，
  延性材料（鋼/木）整根脫離。碎塊本身是**純客戶端視覺**（Q10 條款）。
- Physics Mod 串接（D-032）：先 spike 驗證其 API 可掛性；自家 fallback
  （FallingBlockEntity + 粒子）任何情況都在。上架頁不加前置依賴。

> **2026-09-04 dated 追記（§5 原文不改；裁決 D-046）**：上文「換裝完成後，v0.5 的倒塌判定幾乎是『已在 wire 上』」說的是 tectonic2 的 **v2 wire**
> （`fracture.step`、MC25–MC28），**不是 BSI**——BSI v1 今天沒有 `bsi.fracture` 的動詞，引擎的 `capabilities` 也是空集合。
> 碎塊上 BSI 是引擎 **PE-4**（tectonic2 `docs/V2_PROGRAM.md` §3 軌 B6，其 v2.0-alpha），契約形狀 = `bsi.fracture.step` + `broken` 24 B / `fragments` 136 B
> （commit 軌限定；display 軌 `UNSUPPORTED`），先凍其 `BSI_ADD2.md` 再兩倉同步落地。
> 所以 v0.5 的順序是：引擎 PE-4 → 批次 #2 同步 → 本倉 `BsiRecords` 加型別 → 脫離集合以 `FallingBlockEntity` 群組演出，初速取自 `fragments`。
> §9.5 生命週期 `STRUCTURAL → DETACHING → DYNAMIC → RESTING` 的引擎側對應是 PE-3 的 `lifecycle`（同批）。v1.0 的剛體與 Physics Mod 串接不變。

---

## 6. 風險與未決

| # | 風險 | 對策 |
|---|---|---|
| R1 | ~~bulk 介面無 MPC 可用~~ **已由 D-030 修訂關閉**：未接地拒絕、接地作地，都不需要 MPC；剛體塊列引擎能力需求 | 需求單掛 tectonic；重啟時鄰域標 indicative |
| R2 | J 的解算器是新數值元件 | 住引擎側（T4）。N4 的分級 gate（閉合解主幹 + 夾擠自驗 + 參考級黃金值）；與既有 SHEAR/TORSION 零 oracle 缺口一起處理，順序：先查引擎的 τ 公式再動數字。凹角應力不進 D/C |
| R3 | 稜柱判定的退化與牆柱歧義 | §2.2 收緊後的雙條件（長度 ≥ 2×最大邊 且 截面兩維 ≤ 2）；tie 落到殼/塊。鏡像/旋轉 gate 盯裁決對稱性 |
| R4 | t/L → 1 時 Mindlin 本身失效 | §2.4 的降級門檻（暫定 1/4，凍 gate 時定案）；LE10 只到 t/L≈0.09，**擋不住極端厚**——所以門檻靠理論界線不靠基準通過 |
| R5 | Physics Mod 閉源，API 未知 | spike 限時；結論寫進 D-032 的否證欄 |
| R6 | 大牆的殼數量 × 挫屈成本 | §2.6 門檻先擋；tectonic 另有 Tier 2.5 細長篩（Euler 解析、`buckling.query`），換裝後是更好的常開答案 |
| R7 | **換裝把 v0.4 綁上 tectonic 時程**（D-034 具名風險） | 否證條件 (1) 的備援：T2 凍結的 gates 改在 br-sidecar 實作；兩個發布窗為線 |
| R8 | **殼的雙引擎對數只有帶級**（MC32：drilling 實作自由度） | 桿系照 1e-12 釘；殼在 B2 依 tectonic 的帶級裁決記載，不冒稱逐位；聚合的殼 gates 用閉合解與不變量，不用跨引擎逐位 |
| R9 | ~~tectonic 共線對接缺陷~~ **已關**（T1 落地，2026-08-25） | — |
| R10 | **兩引擎的釘住共線對接慣例分歧**（二審 B）：v0.3b 連通（吞噬+名序）vs tectonic 1d（單側=被釘格中心；兩側=殘樁）。B2 對數在此類 fixture 必紅 | D-035 統一採 tectonic 三條、br-sidecar 以裁決退役不改碼、B2 記名排除；殘餘監控 = 排除清單 ≤ 3 類 |

> **2026-09-02 更正（R6、R7；D-040/D-043）**：R6 對策裡「tectonic 的 Tier 2.5 細長篩……換裝後是更好的常開答案」**是錯的**——
> 細長篩是 per-member Euler 篩（`buckling.h:5-6` 自承 "SCREEN, not eigenvalue buckling"），不是全域 λ_cr 的替代；換裝若只帶篩，
> `bucklingFactor`/N16/N18/D-018 會無聲退役。正確處置：tectonic2 補特徵值 lane（MC66a/b，v1.5），回覆帶 `buckling.kind`，
> 規模政策由宿主以 nodes/dof 決定（Q3(a)）。R7 的時程綁定改以 `docs/SWAP_PROGRAM.md` §6 的版本表為準（v1.3–v1.6）。

**非目標（v0.4 明確不做）**：鋼筋混凝土複合斷面（工法軌）、模板/澆置/養護狀態機、
frame 材料的「肉」（混凝土包鋼）、自由模式對**非結構**原版方塊的推導、任何實體元素求解。

---

## 7. 既有方法對照（M0 文獻掃描，2026-08-24）

**存在性證明**：voxel → 梁+殼理想化有一條完整驗證過的先例管線——骨小樑生物力學的
**ITS-based Plate-Rod FE 模型**（Columbia）：二值體素 → thinning 骨架 → DTA 拓撲分類
（curve/surface）→ 完整體積分解（每個體素標記屬於哪片 plate 或哪根 rod）→ shell + beam
取代，模型尺寸縮小約 762 倍，模量與降伏強度對體素 FE 高度相關。**可行性來自輸入性質
（骨頭處處是薄的），不是演算法通用性**——這正是 D-030 把自動分解限定在 monolith 類別、
塊狀殘餘落到剛體塊的理由：與其在厚塊上硬造模型，不如判定「這不是薄的」然後誠實處理。

**方法選型（三條結論，直接約束 M3 實作）**：

1. **走距離場路線，不走 thinning 路線。** 精確平方 Euclidean 距離轉換
   （Saito–Toriwaki / Felzenszwalb–Huttenlocher）在整數格點上是精確整數運算——
   決定性與鏡像不變免費；局部厚度（Hildebrand & Rüegsegger）就是 CAD 降維
   「厚度比判準」在體素上的直接對應。反之 subiteration thinning 依固定方向順序掃描，
   有方向偏置，鏡像不變性不保證。§2.2 的逐軸 run 長分類是局部厚度的軸對齊簡化版，
   與此路線相容。
2. **線/面分類的嚴格版已存在**：Saha 等人的 digital topological analysis（DTA）
   用 3×3×3 鄰域的 tunnel/cavity 數把骨架體素分成八類（曲線/曲面/端點/接頭）——
   純局部、基於拓撲不變量。v0.4 的簡化分類若在畸形輸入上出現裁決歧義，這是升級路。
3. **業界公認的難點在接頭不在分類**：CAD midsurface 工具（CADfix、HyperMesh）的
   失效模式是 ill-connected midsurface（缺片、縫、重疊），修補以天計。本計畫把
   L/T 縫合（§2.2、N6）與 bulk 介面（R1）列為主戰場，與文獻的教訓一致。

**任何貪婪掃描（含 maximal-box 分解）的 x→y→z 順序都會破壞鏡像不變**——除非裁決
正則化。這把 N2 從「驗收項」升格為「設計約束」：每一條分解裁決都要先問「鏡像後這條
裁決還做同一個決定嗎」。轉角質量 50/50 均分是這個約束的第一個實例。

**遊戲界對照（查證於 2026-08-24）**：Medieval Engineers 是逐方塊應力啟發式 + 連鎖崩塌
（演算法未公開）；Space Engineers 明確不做；Teardown 是程序化碎裂非結構分析；
既有 Minecraft 結構 mod（Realistic Block Physics 等）是鄰居計數/支撐距離啟發式，
無勁度矩陣；唯一真 FE 的 Minecraft 案例（Purdue 教學工具）是體素即元素的連續體，
自承粗解。**對外文案的合規寫法**：「據我們所查，尚無遊戲以梁殼理想化的 FEA 做結構
判定」——帶查證日期，不寫絕對句；M7 時進 check_docs.py 禁句表管理。

**文獻掃描第二批（斷面與厚板，2026-08-24）**——結論已分別寫入 §2.3、§2.4、N4、N5。
補充事實與待確認：

- sectionproperties（MIT）的方法是 **warping function FEM**（Tri6，Lagrange multiplier
  處理純 Neumann 零空間），不是 Prandtl；它做 warping 前強制檢查斷面連通——「連通性
  是 J 的必要條件」與本專案不變式 4 是同一件事的兩面。
- 矩形 J 的精確級數出自 Timoshenko & Goodier《Theory of Elasticity》；本檔 §2.3 的
  六點表由該級數計算。
- NAFEMS LE10 的 t/L ≈ 0.09，屬中厚——它驗證元素，**不驗證極端厚的理論效力**，
  所以 §2.4 的降級門檻獨立於它。
- 待確認清單（凍 gate 前結案）：FSDT Navier 式對 Reddy 教科書複核；FD 版 Prandtl 是否
  嚴格下界；sectionproperties 的 Av 與 Cowper 的實際差距；NASA TP-3608 是否含 L 型
  高精度 J 值（PDF 未取得）。

### 7.5 tectonic 現況查證（2026-08-25，D-034 的事實基礎）

- **使命線**：ROADMAP 明載 ArchSim 已棄置、消費者 = block-reality、v4 = 換裝 +
  FrameCore 退場。M-MC1…M-MC4 全部出貨：D-010 語意抽取（宣告軸向、
  焊接、養護、fill）、兩軌 Session、world.edit 三級、C4 ledger/replay、容量面 + 碎塊 +
  Newmark + 衝擊、**換裝 artifact `frame_capi_v2.dll`**（200 輪 wire soak 逐位）、
  對 FrameCore 桿系 1e-12 對數（殼帶級，drilling 自由度）。v1.1 = per-island 帳
  （51K 城鎮單塔熱圖 2.33 ms）。套件數是移動目標：本節初稿時 MC1–MC41，
  二審時點已至 **T1..T39 + MC1..MC42**（v2 U4/U6、T41 持續前進）——引用時以
  當下 `git log` 為準，不抄本節。
- **收斂處**：`McBlock.axis` 與 D-029 同構同理由；雙倍座標與 D-028 半格單位同構；
  vocab 自述「與 Minecraft 側唯一刻意耦合 = 詞彙表（產品概念，永不是 solver 概念）」
  ——與不變式 7 同一條。
- **缺陷（程式碼追蹤級，T1 處理）**：`core/mc/extract.h` 的同軸對接讓兩條 run 互相
  延伸到**對方的方塊中心**——兩個中心是兩個節點，產出兩根重疊一公尺、不共任何節點
  的成員；斷面變更的柱子上半截斷連。跨軸 butt-extension（MC10 有力分配 oracle）正確；
  壞的只有共線同軸支。對應 gate（mc_exit MC2「section change: 2 members, shared
  node, 2 sections」）只數個數、未斷言共節點。block-reality 的 D-025→D-028 是同族
  先例：修法 = 節點放共享面。
- **它還沒有的**：monolith 聚合（vocab 只有 Member/Panel/Support 角色）——正是 T2/T3
  要送進去的東西；剛性連結/MPC（D-030 修訂後短期不需要）。

### 7.6 T1 落地與 B1 spike（2026-08-25）

**T1 已落地**（tectonic2 PR #6，rebase 進 main `44d7632`）：判準凍結（spec §1d）→
面節點實作 → MC2 升 22 條斷言（含「柱底 N = ρAg·3.5 對 1e-9」的物理 oracle——斷連的
舊抽取不可能給出）→ 六套全綠。R9 關閉。

**B1 spike（換裝傳輸路徑實證）**：ctypes 客戶端驅動 `frame_capi_v2.dll`（sha 44d7632，
含 T1）完成 open → hello → world.declare（40 B 方塊記錄）→ solve.request → 9 B/塊
utilization 讀回。單柱**往返 0.2 ms**；D/C = 0.002771 Axial 與 ρAgL/(A·σ_allow) 手算
一致；`frame_v2_abi_version()=2`、engine 1.1.0。傳輸與框架層零障礙。

**B1 凍判準前要解的四件事**（依實測記錄，不是猜的）：

1. **詞彙固定 `Vocab::builtin()`（v1 明文）**——block-reality 的材料表（D-012 值：
   steel/rebar/concrete/timber/brick）推不進 wire。引擎需求：vocab datapack 動詞，
   與 T2 的 monolith 詞彙一起議（monolith 本來就要動 vocab）。
2. **無 vocab 查詢動詞**——材料/斷面 id 映射得靠 host 寫死。需求：hello 或查詢動詞
   曝露詞彙版本與 id 表，否則 host 與引擎建置隱性耦合。
3. **單位**——wire 端 SI（公尺、Pa 系），block-reality 端 mm/MPa（D-012 慣例）。
   host 轉換恰一次，gate 用閉合值釘。
4. **回覆形狀是 B1 的主設計題**——tectonic wire 只回 per-block D/C＋主導模式＋diag
   計數（不變式 7 的純化形），而 block-reality 現行 Java 消費 member 級明細
   （stations/forces/equilibrium/bucklingFactor）。B2 對數以 per-block 軌為準；
   明細軌二選一：引擎加 readback 動詞，或 Java 面收斂到 per-block。傾向前者
   （hover 的教學價值靠 member 明細），T2 時一併議。

---

## 8. PR #60 審核處置紀錄（2026-08-25）

八條全數成立，逐條處置；無一被駁回。

| # | 審核發現 | 處置 | 落點 |
|---|---|---|---|
| 1 | 稜柱先判會把 6×4×1 牆判成梁 | 成立。條件收緊為雙條件（長度 ≥ 2×最大邊 **且** 截面兩維 ≤ 2） | §2.2-1、R3 |
| 2 | L/T 轉角剛度重複計算 | 成立。網格止於中面交線；殘餘重疊照登 indicative；質量與力平衡維持精確 | §2.2-2、N6 |
| 3 | 非對稱斷面缺 Iyz/主軸 | 成立且重要。慣性張量取主軸 + 局部軸旋轉進引擎；新 gate N12 | §2.3、N12 |
| 4 | 整片 monolith 一個物件太粗 | 成立，但解法是分層不是拆物件：物件=聚合/呈現單位，損傷記元素與方塊層，斷裂切新物件 + lineage | §2.5 |
| 5 | frame T/X/L 接頭規則不完整 | 成立。補完：跨軸→中心共節點；同軸對接→面節點（D-028），並指名 tectonic 現行規則的缺陷 | §2.1、§7.5 |
| 6 | bulk 剛體會撐硬鄰域 | 成立，與 D-033 交會後由產品重裁：未接地拒絕（理由碼）、接地作地、剛體列引擎需求 | D-030 修訂、§2.2-2 |
| 7 | 缺 chunk/增量規則 | 成立。分析範圍=registry 持久區域，載入狀態永不觸發重分類；新 gate N11 | §2.5、N11 |
| 8 | 與 MEMBER_SEMANTICS 衝突 | 成立。「混凝土不產生元素」限縮於工法路徑的澆置混凝土；monolith 是 §9.3 的完成式，已寫進該檔增補 | `MEMBER_SEMANTICS.md` §7 增補 |

同批入檔：D-033（Java 零力學/adapter 不仿造/邊界中立）、D-034（換裝為主軸），
里程碑改組為 T 軌（tectonic）+ B 軌（block-reality）。

### 二審處置（2026-08-25，`V04_PLAN_REVIEW_2026-08-25`，審核對象 `7233d77..638cbde`）

七條發現 + 五條低嚴重度，**全數成立、全數採納**；其中兩條關鍵事實我重測證實
（bucklingFactor 兩態塌縮、釘住對接兩引擎模型不同），一條經 fetch 證實（MC42）。

| # | 發現 | 處置 |
|---|---|---|
| A 🔴 | 不變式 2 改寫未登記、無下游降級 | GATES.md 補登（D-030 列）：v0.1a–v0.3b 混凝土/磚 D/C、自重、挫屈值於 v0.4 落地時作廢（倍數級） |
| B 🟠 | T1 已完成（計畫過時）＋兩引擎釘住慣例分歧 | 前半 PR #62 已處理（審核對象在其之前）。後半實測證實（我測兩側釘住 = 連通 4 members/1 island；其 3 members 為 fixture 差異，不影響結論）→ **D-035**：統一採 tectonic 三條、br-sidecar 以裁決退役不改碼、B2 記名排除、GATES.md 登記、新 R10 |
| C 🟠 | 稜柱「兩維皆 ≤2」誤傷 3×3×20 方墩；bulk-ground 無上限 | (b) 改**截面長寬比 ≤ 3**；bulk-ground 加規模上限（候選：垂直 ≤ 2 格；T2 凍） |
| D 🟠 | N1 與 frame 自由端慣例衝突，D-030 否證 (2) 會誤射 | N1 限 monolith；N13 分軌記帳（frame = Σ ρ·A·L） |
| E 🟡 | 「三態語意 v0.3b 已修好」不實 | 實測證實（關閉與受拉皆 0、僅一 key）。§2.6 更正；B3 加顯式理由欄 |
| F 🟡 | §0 的 L/h=1 是 1 m³ 實心的數字 | 改用宣告斷面真值 1.67／2.86／2.50，論證不變更有力 |
| G 🟡 | D-029(1)/D-031(1)/D-032(2)/D-033(1) 否證條件無門檻 | DECISIONS 各補修訂：樣本量、件數、時窗、程序化條款 |
| §3 | MC42、受測者=宿主對、挫屈成本錨（400≈4 s）、稜柱等號邊界、M0 單方查證 | 全數入文（§7.5、§2.8、§2.6、§2.2、§5） |
