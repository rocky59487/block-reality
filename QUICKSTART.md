# 上手

安裝就是把一個 jar 丟進 `mods/`——引擎在 jar 裡面。`blockreality-0.3c.zip` 是完整發行包
（`dist/` 是它的內容），裡面另外附上獨立的引擎與雜湊，供查驗或在遊戲外執行求解器。

---

## 0 · 安裝與啟動

### A · 已經有 Minecraft 1.20.1 + Forge 47.x

**把 `blockreality-0.3c.jar` 丟進 `mods/`，開遊戲。** 分析引擎在 jar 裡面，第一次要用到時
自己解到 `<遊戲目錄>/blockreality/engine/<雜湊>/`。全程不下載任何東西；雜湊對不上就拒絕
執行。（macOS 或 ARM：尚無預建引擎，mod 可玩、分析關閉並說明原因；要分析請自行建置
`br-sidecar` 並用 `sidecarPath` 指向它。）

zip 裡的 `install.bat` / `install.sh` 是選配的，做的事一樣、外加複製一份獨立引擎。不帶參數
執行時會在原生啟動器、Prism、MultiMC、Modrinth、CurseForge 的慣用位置尋找實例；找到多個
會列出來讓你選，而不是自行決定。也可以自行指定遊戲目錄，或用 `--list` 只列出找到的實例。

### B · 沒有 Minecraft，只想看它執行

```
Windows：  run.bat
Linux ：   ./run.sh
```

啟動開發用客戶端，只需要 PATH 上有 JDK 17。Gradle 來自 wrapper，Minecraft 與 Forge 由
ForgeGradle 於首次執行時取得，需要數分鐘，只有一次。

> Minecraft 與 Forge 不可轉散布，因此無法提供解壓縮即可遊玩的完整包。B 是唯一能一步
> 啟動遊戲的路徑。

zip 內容（約 2.4 MB）：

| | |
|---|---|
| `blockreality-0.3c.jar` | Forge mod，api + core + impl 與**兩顆引擎**全在裡面——光這個檔案就是完整安裝 |
| `br-sidecar.exe` | 同一顆 Windows 引擎的獨立檔，只 import KERNEL32 與 msvcrt，無額外 DLL |
| `br-sidecar` | 同一顆 Linux 引擎的獨立檔，只依賴 libc/libm |
| `install.bat` / `install.sh` | 選配安裝器 |
| `START-HERE.txt` / `讀我-中文.txt` | 說明文件，英文與中文 |
| `SHA256SUMS.txt` | 每個檔案的 SHA-256 |

> FrameCore 靜態連結進 `br-sidecar`，沒有 FrameCore.dll 這種東西。`br-sidecar` 是獨立
> 程序而非 mod 載入的函式庫（D-013），因此 C++ 端若發生錯誤，代價是一次分析失敗，
> 不是整個伺服器與存檔。

打包與發版流程見 `docs/RELEASING.md`。

---

## 1 · 自己重建引擎（想改 C++ 才需要）

**依賴只剩 Eigen**（header-only）。METIS / OpenBLAS / LAPACKE 都不用了——
FrameCore 的 supernodal lane 由 `FRAMECORE_SUPERNODAL` 在編譯期關掉，而
`useSupernodalPrimary` 本來就是 `false`，所以每次求解走的一直都是 Eigen LDLT。
**實測開與關的數字到最後一位都相同**（68/68 逐位元，當次比對的 gate 全數通過）。

**先套引擎補丁——跳過這步必然編譯失敗。** 上游 FrameCore v4.0.0 是凍結的，
本專案對它的三個修正以 patch 系列隨倉攜帶（`sidecar/patches/`，緣由見其 README
與 D-020）。`main.cpp` include 的 `ShellEdgeRecovery.h` 就是 patch 0002 新增的檔案，
所以對著未套補丁的 v4.0.0 建置，第一個錯誤就是找不到它：

```bash
cd /path/to/architect_simulator
git checkout v4.0.0
git am /path/to/block-reality/sidecar/patches/*.patch
```

然後才是建置：

```bash
sudo apt-get install -y libeigen3-dev cmake g++

cmake -S sidecar -B sidecar/build -DCMAKE_BUILD_TYPE=Release -DBR_STATIC_RUNTIME=ON \
      -DFRAMECORE_DIR=/path/to/architect_simulator/Plugins/FrameSolver/Source/FrameCore
cmake --build sidecar/build --parallel
```

**確認：**

```bash
python3 sidecar/verify.py sidecar/build/br-sidecar
```

最後一行要是 `ALL PASS`（282 項，含傳輸等價與 fail-closed 協定 gate）。**這一步失敗就不要往下走**——後面看到的任何東西都不可信。

一次把全部（Linux + Windows + jar）打包好：

```bash
scripts/package.sh /path/to/Plugins/FrameSolver/Source/FrameCore
```

Windows 那顆要 `apt-get install -y g++-mingw-w64-x86-64`；沒裝就只出 Linux 版。

---

## 2 · 確認 Java 那條線通

```bash
cd mod
gradle test -Dbr.sidecar=../sidecar/build/br-sidecar
```

全部測試 0 failures。其中 28 個會**實際啟動 `br-sidecar` 子程序**跑真的 FrameCore（含兩條 JSON↔共用記憶體等價測試）。

沒帶 `-Dbr.sidecar` 這 28 個會 skip 而不是假裝通過。**帶了但路徑不對則直接建置失敗**，
不會 skip——先前那一行相對路徑在 forked test JVM 裡解析不到，測試全部靜靜 skip 而
build 照樣 SUCCESSFUL。靠「不跑」變綠是最糟的一種綠。

---

## 3 · 進遊戲

```bash
./run.sh          # 或 Windows 的 run.bat
```

引擎路徑**不用設環境變數**。`SidecarLocator` 依序找：

1. `config/blockreality-server.toml` 的 `sidecarPath`
2. `-Dbr.sidecar` 系統屬性
3. `BR_SIDECAR` 環境變數
4. **jar 內建的引擎**，首次使用時自解到 `<遊戲目錄>/blockreality/engine/<雜湊>/`
5. `<遊戲目錄>/br-sidecar` 或 `<遊戲目錄>/blockreality/br-sidecar`
6. `PATH`

前三項是你親手指定的，永遠優先。第 4 項排在第 5 項**前面**是刻意的：遊戲目錄裡的
`br-sidecar` 最常是舊版安裝器留下的那顆，內建的才保證和正在跑的 jar 同一版。自解
目錄以引擎的 SHA-256 命名，更新會落在舊引擎旁邊而不是覆寫它，寫到一半的檔案也
不會被誤認成完好的引擎。

`run.bat` / `run.sh` 幫你放好的是第 5 項那顆——開發用客戶端從原始碼直接跑，沒有
內建引擎的 jar（引擎要 `scripts/package.sh` 才放得進去），所以這條流程實際用到的
正是它。

---

## 進去以後做什麼

創造模式，物品欄搜 `Block Reality`。九種結構方塊加**應力眼鏡**；先拿最基本的兩種上手：

樑與板是**兩種元素**，不是同一種東西換個顏色（完整方塊表見 README）：

| 方塊 | token | 元素 | 尺寸 |
|---|---|---|---|
| 結構鋼 | `steel_rect_200x400` | 6-DOF 樑 | 200 × 400 mm 斷面 |
| 混凝土樓板 | `concrete_slab_200` | MITC4 板殼 facet | 200 mm 厚 |

**由 token 決定，不由形狀猜。** 用結構鋼鋪一層樓板不會變成板，會變成**格梁**——那也是一個
合法的模型，只是每個方塊的自重與勁度都被算了兩次（一條 X 向、一條 Z 向）。想要板就用板。

### 接地判定

一個結構方塊被判定為**接地**，條件只有一個：它**正下方**那一格是實心且非結構的方塊
（`StructureManager.isSupported`）。除此之外沒有別的接地方式——**從側面頂著一面牆不算**，
那樣整組會被正確地判為機構。

### 最小案例：懸臂

1. 蓋一面**五格高的石牆**
2. 在**牆頂**放一格結構鋼，再往外接**四格**排成一直線伸進空中
   - 第一格踩在石頭上，因此接地；其餘四格懸空
3. 拿**應力眼鏡**
4. **蹲下右鍵最外面那一格** → 加 20 kN 測試荷載（同一個動作可以再移除）

**應該看到：** 沿著梁有一條線，顏色是**利用率**（冷 → 琥珀 → 朱紅）。**看向那根梁**，
左上角就會出現它的**斷面圖**：一條垂直軸、往兩側伸出的應力剖面、中性軸的位置，
以及**用文字寫出來的**「上緣 受拉 +24.24 MPa／下緣 受壓 −24.24 MPa」。

### ⚠️ 「上拉下壓」只對懸臂成立

這是我上一版文件寫錯的地方，**很可能就是你看到「相反」的原因**：

| | 變形 | 上緣 | 下緣 |
|---|---|---|---|
| **懸臂**（一端固定、另一端懸空） | 上凸（hogging） | **受拉** | 受壓 |
| **兩端有支承的梁**（樓板、橋） | 下垂（sagging） | **受壓** | **受拉** |

**兩個都是對的。** 上緣是拉還是壓**不是梁的性質**，是結構形式的結果——而那正是這個工具要告訴你的事。

所以現在 HUD **用文字寫出來**，不要你從顏色反推。要純文字對照就打 `/br section <id>`，
它會把每一站的 `+/−` 與 `TEN`/`COM` 列出來。

### 蓋一片樓板

1. 平鋪一片 **7×7 的混凝土樓板**
2. 四個角往下接**結構鋼柱**到地面

**應該看到：** 樓板的每個外露面都有等值圖。上表面畫的是板的上表面應力，**走到樓板底下**
看到的是下表面——受彎的板兩面是等值反號的，所以那是答案的另一半。**側面沿高度**從下纖維
掃到上纖維，中性面就以中性色帶出現在它真正的高度。

看向樓板，HUD 會給板的讀數：厚度、D/C（表面 von Mises）、上下表面的主應力。**沒有「控制
纖維」這一欄**——板沒有纖維也沒有中性*軸*，借樑的詞彙來講板是不對的。

> 這片板大概會**超過容量**（D/C ≈ 1.9），那是對的：素混凝土抗拉只有 3 MPa，6 m 跨的板
> 需求約 6.4 MPa。它本來就該裂。要它站得住就縮小跨度或多加柱子。

### 細長柱子：應力說安全，穩定說不安全

蓋一根**很高很細**的柱子（20 格），在頂端加荷載。

HUD 會多一行 `挫屈 λ_cr`。這是**當前載重的倍數**，到了它結構就失穩，所以 λ_cr ≤ 1 表示
**已經**過了。實測 20 m 鋼柱受 400 kN：`max D/C = 0.0187`（應力看起來非常安全），
`λ_cr = 0.63`（已經失穩）。

**兩個都是對的，而且它們回答不同問題**：一個是材料強度用完了，一個是幾何剛度用完了。
Euler 臨界載重隨長度**平方**下降，而應力比根本不知道長度存在。把同一根柱子縮短到 8 m，
λ_cr 回到 4.6。

> 這是線性挫屈的**起始點**，它不告訴你失穩之後會怎麼樣。誤差往哪邊偏要看網格：一段共線
> 方塊是**一個**樑元素，除非有載重或接頭逼出內部節點。頂部大載重（軸力接近均勻）時單元素
> 準到 0.5%，此時初始缺陷與後挫屈軟化讓它是**上界**；但**自重主宰**的細長柱相反——19 m
> 柱回報 3.14，精確解 9.89，**低 68%**，偏保守。同一根柱在半高處加一個一牛頓的載重，倍數
> 會跳升 68%（run 被切成兩段）。要自己看：`python sidecar/repro_selfweight_buckling.py`。

### 蓋一面剪力牆

把混凝土樓板**豎起來**貼成一面牆（例如 5 寬 × 13 高），底排接地，然後對牆頂的方塊下

```
/br load 30 0 0
```

——對你**注視**的方塊施加 30 kN 的水平測試荷載（單位是 kN,三個分量是 Minecraft 的
x/y/z 軸）。`/br unload` 移除,`/br loads` 列出所有荷載,眼鏡的蹲下右鍵仍是「向下
20 kN」的快捷鍵。

這是當初選 MITC4 而不是格梁化的**唯一理由**（D-005）：格梁在每個節點固定面內自由度，
對這條荷載路徑會回報**恰好零**。牆同時用兩種方式抵抗：面內剪力流 V/w，以及當成懸臂梁的
傾覆軸力。兩者對閉合解都是 1e-7 等級。

### 換一種材料蓋

創造分頁裡有九種結構方塊：三種鋼斷面、素混凝土樑、木樑、磚柱、兩種厚度的樓板、鋼板。
值得做的實驗：

- **同一支懸臂換三種鋼斷面**——150x300 的斷面模數只有 200x400 的 42%,同樣的載重
  先變紅的是誰,一眼就看懂「斷面模數」在幹嘛
- **素混凝土樑**受拉 3 MPa 就到容許值——懸臂根部上緣先「裂」,這正是為什麼真世界
  要放鋼筋（RC 複合斷面還沒做,token 誠實地叫 `concrete_rect_400x600`）
- **磚柱**只該受壓。把它當樑用,HUD 會用 TENSION 告訴你磚有多不喜歡這樣

### 把荷載加在梁的中間

現在可以了。被加荷載的方塊會變成節點，run 在那裡切開，荷載就落在你放的地方。

> 以前這會**拒絕整個請求**——也就是說在梁中間掛個重物，全世界的分析都停掉。

### 好幾棟建築

以前這會讓整個 mod 看起來壞掉：**任何一棟沒支承，全世界都不回報**。現在每一棟各自求解，
`/br status` 會寫 `structures  3 solved, 1 unrestrained`，沒問題的那幾棟照常畫。

### 三個鏡頭

**右鍵空氣**切換：`利用率` → `應力` → `材料`。

- **利用率**（預設）— 每根構件一條線一個顏色，依 D/C。**這是複雜建築唯一有用的視圖**：
  它回答「哪裡快壞了」
- **應力** — **只有你正在看的那根**會畫出四條纖維帶與中性軸。全部一起畫在真實建築上
  是一團霧，回答不了任何問題

### 故意弄壞

- **不接地**（懸空放）→ HUD 說「這是機構不是結構」。**這不是 bug，是正確答案**：沒有東西撐住它，就沒有應力可以報。
- **加很大的載重** → `max D/C` 超過 1，變紅。
- **中途換斷面** → 變成兩根共用一個節點的構件。

---

## 沒有客戶端也能測（我就是這樣驗的）

`gradle runServer` + RCON，不需要顯示卡。上面那些數字就是這樣量出來的。

```bash
# forge/run/server.properties
enable-rcon=true
rcon.password=<自己設>
rcon.port=25575
```

然後 `/setblock` 擺方塊、`/br scan` 讓 mod 認得它們（指令擺的方塊不會觸發 place event）、
`/br members` 看結果。

## 出問題就打 `/br status`

它會告訴你引擎在哪、**找過哪些地方**、目前 revision、幾個方塊、上一次結果是什麼。

```
/br status         引擎狀態 + 找過的路徑 + 上次結果
/br members        每根構件的 D/C、控制纖維、控制斷面位置、峰值應力
/br scan [半徑]    重掃已載入的區塊（指令/世界編輯擺的方塊用）
/br resolve        強制重算
/br reset          引擎被停用後重新啟動
```

會改動狀態的四條指令——`load`、`unload`、`scan`、`resolve`——以及 `reset` 需要
OP 權限 2。單人創造世界本來就有；生存模式或未開作弊、以及伺服器上非 OP 的玩家，
這幾條連指令樹都不會出現。**應力眼鏡不需要任何權限**：它施加的是一個設定好的
向下測試荷載，屬於玩法動作；`/br load` 可以給任意方向、任意大小的向量，不是。

**沒有引擎也能玩。** Mod 正常載入、方塊照放，只是不分析，而且會說原因。

---

## 驗證證據

`evidence/VERIFICATION.md` 是**由 `scripts/evidence.py` 產生的**，不是手打的表格
（它**不在** release zip 裡——發行包只放能玩所需的東西，證據留在倉庫，這是
package.sh 自己註明的取捨）。每一個數字都來自一次可重複的執行，並蓋上引擎
commit、二進位檔 hash、原始碼 hash 與主機；**provenance 查不到（commit 不可解析、
工作樹不乾淨）時 evidence 的 gate 直接紅**，不會再出現蓋著 `unavailable` 卻照樣
PASS 的紀錄。

| | |
|---|---|
| 對閉合解（非零參考） | **31 項，最差相對誤差 1.2e-14**（前兩版引用的 1.6e-10 是舊 wire 的 10 位截斷，不是引擎） |
| 對恰為零的參考 | 10 項，最差絕對殘差 **1.49e-08 N·mm** |
| 無閉合解的性質 | **12/12**（機構偵測、單方塊、荷載拒絕、缺料拒絕、未知斷面拒絕、重複求解逐位元相同、島隔離、板不是格梁、實心板拒絕、支承還原不會降低需求、荷載切開構件、細長柱應力安全但挫屈失穩） |
| 板元素收斂 | 跨中 20 元素 **0.57%**；支承還原後 **2.7%**（逐角原值 18.9%） |
| 剪力牆 | h/w≥5：剪力流 **1.4e-07**、傾覆 **~1e-09**；h/w=3：**2.7e-05 / 6.8e-07**；方牆是深樑，樑理論本身不適用（1e-3～2e-2 是參考模型的誤差） |
| 線性挫屈 | 單元素柱對課本值 **1.6e-05**；臨界載重的 1/L² 關係 **2.3e-10** |
| 跨平台決定性 | **8/8 逐位元相同**（Linux 原生 vs Windows 交叉編譯，比對整行回覆） |

每一次求解的回覆裡都自帶一個**全域平衡殘差**——施加載重由幾何與密度獨立重算，不是從組裝後
的載重向量讀回來，所以它是一句與求解器無關的陳述。`/br status` 直接印它。

**兩個誤差指標分開報是刻意的**：恰為零的量沒有相對誤差，把它折進同一個數字會把其他
所有比較都誤報成 1e-8 等級，而實際上非零比較好了好幾個數量級。

效能（含序列化與程序邊界的完整往返；十五次取中位數與最小值，機器是一台有雜訊的
筆電——同表也印最小值，兩者差距就是雜訊的大小，照登不修飾）：

| 構件數 | 自由度 | 中位數（預設，含挫屈） | 最小值 |
|---:|---:|---:|---:|
| 1 | 12 | 0.29 ms | 0.28 ms |
| 19 | 120 | 4.5 ms | 4.1 ms |
| 99 | 600 | 23.4 ms | 22.4 ms |
| 199 | 1200 | 50.4 ms | 48.5 ms |

**199 根構件約 50 ms**——而且求解不在 tick 執行緒上，所以這是「拿到結果的延遲」
而不是「從遊戲拿走的時間」。傳輸本身的成本另有專節：同一個 86 構件模型，
共用記憶體 2.3 ms、JSON 15.7 ms（省 85%）。

> 這張表是**單次取樣**，每次 `package.sh` 重測。上一版同樣的 199 構件案量到 117 ms，
> 這一版 50.4 ms——差不多 2.3 倍，差別是機器當下的負載，不是程式。比值（省 85%、
> 挫屈加成兩成以內）跨版穩定，絕對值不穩定；要當數字用請看
> `evidence/VERIFICATION.md` 裡當次的那張表。

線性挫屈是**預設開啟**的第二次求解。純應力檢核對細長構件不安全，所以它開著；
它的代價在 evidence 的表裡逐尺寸量出（雜訊之內，普遍在兩成以下）。請求裡帶
`"buckling": false` 可以關掉。

`scripts/package.sh` 會在打包前跑這份證據，**沒過就不出貨**。

---

## 🔴 誠實邊界

**伺服器端已經在真的 Minecraft 裡跑過**（Forge 專用伺服器 + RCON，見 `forge/README.md`）：
mod 載入、指令、sidecar 自動尋找與啟動、真的 FrameCore 求解、機構偵測、乾淨關閉，
log 裡 0 個 ERROR、沒有殘留程序。上面那些應力數字都是實測並手算對過的。

**Windows 引擎已經實際跑過** — 用 Wine 跑完整 gate 全過，而且逐行回覆跟 Linux 版
**8/8 逐位元相同**（比對的是整行回覆，不是挑欄位）。

**客戶端渲染已經在真的遊戲裡看過了。** README 上那兩張就是實機截圖：表面等值圖、圖例、
HUD 的構件與板計數、看向構件時的斷面圖與中性軸都有畫出來。資料層另外被 215 個測試釘住
（顏色、位置、符號、梯度、中性軸）。

畫面不對時，按可能性排序：

1. **看不到等值圖** — 纖維在方塊**內部**（斷面 200×400 mm，離中心線只有 0.2 格），
   所以渲染刻意關掉深度測試。如果還是看不到，先確認手上拿著應力眼鏡、`/br status`
   說引擎 READY、而且 `/br members` 有東西
2. **整條位置偏移** — `RenderLevelStageEvent` 的相機位移沒對上
3. **HUD 沒出現** — `RenderGuiOverlayEvent.Post` 的 overlay type
4. **顏色反了** — 資料層有測試釘住，如果真的反了，問題在渲染端而不是資料

有任何一項不對，把 `/br status` 的輸出跟 `logs/latest.log` 貼出來。
