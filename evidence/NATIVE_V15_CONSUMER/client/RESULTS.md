# Real Linux client recheck

Source98dbd54; production classes match the qualified jar's1a60418 sources. Uses
ordinary Forge development runs with opt-in server/client observation probes,
the exact published v1.5 SO, BUNDLED extraction and the actual game socket.
This is not an installed-client test. Rendering is llvmpipe software.

The unchanged nine-product oracle passes127 actual model/atlas/target/collision
checks. Existing16 scenes pass45 HUD/framebuffer/native-value/revision assertions.
The real client receives cached native revision44 with2 members/12 plates before
editing. Actual vanilla client interaction passes14 placement/axis-cycle/ray checks.
Six extra catalogue/hotbar/inventory captures pass their size/revision checks;
22 PNGs total. The final sequence only defers the original scene coordinator's
exit until geometry interaction/capture checks complete. No native reply or accepted
model state is injected. Server and final client both exit0 normally.

Visual inspection covers1280 Chinese beam/panel/refusal HUD,1920 unscanned product
catalogue and inventory: readable native values, non-overlapping vanilla toast,
wrapped refusal, narrow section silhouettes, distinct end faces and visible
undeclared marks. The existing module refusal detail is still English in the
Chinese HUD; panel material cells are still full cubes because placement axis is
not the physical panel normal. These remain UI/product limitations, not newly
qualified fully resolved panels. No FPS or Windows CM/N25 claim.

Gate6's geometry/HUD regression passes. Shell-indicative warning from an actual
game input remains UNOBSERVED and OPEN. The shipped fixtures do not trigger it;
the direct CAPI/JNA C14 warning checks are recorded separately, never relabeled
as a game observation. This conditional part of gate6 is not silently waived.

First setup failures remain: the copied source archive omitted root gradle/,
so the first server launch failed before game load; adding the exact tracked
Gradle input allowed the unchanged module to build. The first client used a new
profile and stopped at AccessibilityOnboardingScreen; the model coordinator
timed out without connecting. An attempted correction expected an options.txt
that vanilla had not written, and an incorrectly started follow-up client again
stopped at onboarding. Both clients exited normally by the probe's owned exit
command; neither counts as a successful observation. The final profile copies
the prior qualified MG probe options (onboardAccessibility:false), with source
path/hash retained, without modifying that prior profile. The missing-file
exception was observed in the tool output; there is no fabricated raw exception
file. Raw launch logs/states and the failed model timeout are in the ZIP.

`raw-observations.zip` contains125 raw files and an internal per-file SHA manifest,
including all3 observation directories,22 PNGs, first failures, exact options,
commands and generated/cached SO identities. Game world/chunk data, duplicate
native libraries, Gradle caches and Python __pycache__ are not included.
`receipts.json` describes this archival selection. The original roots remain at
/home/rocky/br-native-v15-render. Original Windows CM profile/dialog/server remain
untouched. CI atf7f9533/run34441250786 passes4 jobs with15 native steps SKIP;
this qualifies that exact head, not an engine build or a later unchecked head.
